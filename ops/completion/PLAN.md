# Completion — Operational Plan (the track that finishes the proposal)

Three implementation tracks are complete — Clang backtick `S00`–`S12`, GCC
backtick `G01`–`G10`, Clang Unicode `U00`–`U21` — and a fourth,
`ops/backlog/PLAN.md`, closed the four defects that most needed a compiler.
What is left is not more implementation. It is **making the two papers say
what the implementation actually established, and making every claim in them
checkable.**

This plan covers *all* of it: 31 open `BNN` rows, 29 unreconciled deviation
rows, 7 open design decisions, and the two papers themselves. Nothing in
`ops/` is outside it.

## Why this is not ordered by severity

`ops/BACKLOG.md` §7 orders the defects by how badly they hurt, and
`ops/backlog/PLAN.md` scheduled the top of that list. That was the right
order while the question was "which defect most damages the prototype". It is
the wrong order now, because the prototype is done and the remaining question
is different: **which of these changes a paper?**

So every item below is placed by what it does to a paper, and the phases are
four questions asked in order:

1. **What must be decided before anything can be written?** Seven questions
   are open, four of them with implementation consequences. Deciding them
   late means implementing twice and writing twice. This is the critical
   path and it was in no plan at all.
2. **What does a paper currently say that is false?** Paper-truth defects.
   [`type-slot-implementation`](../BACKLOG.md#type-slot-implementation) was the last one anybody had scheduled; there are more.
3. **What did the implementation learn that no paper says?** 29 deviation
   rows, of which the Unicode ledger's 24 have *never* been reconciled. This
   is the largest single body of remaining work and the most perishable —
   every row was written by an agent who is gone.
4. **What does a paper claim to have measured that was not measured?**
   Evidence debt: unbuilt hunks, untestable branches, tables that cannot be
   regenerated.

Two bodies of work gate nothing and can run at any time: **upstream
citizenship** (defects that are LLVM's, not ours) and **hygiene** (dead code,
cosmetics, parity gaps with no paper consequence). Upstream is placed *first*
anyway, because a paper that says "we found a Clang bug" is stronger when it
can cite the issue number, and issues take calendar time to be triaged.

## How to use this plan
1. Read `ops/AGENT_PROTOCOL.md`. Substitute for this track: plan → *this
   file*; step files → `ops/completion/steps/CNN-*.md`; handoffs →
   `ops/completion/handoffs/CNN-<slug>.handoff.md`; deviations → the ledger of
   whichever track the step touched.
2. Find the first unchecked step whose dependencies are all checked.
3. Execute only that step. Then stop.

## Ground rules

The four from `ops/backlog/PLAN.md` still hold — one step per agent, no green
no check, everything gated behind `-fbacktick` / `-funicode-operators`,
minimal diffs — plus four that are specific to this track:

- **A docs step has a gate too.** It is not `check-clang`; it is that every
  row the step claims to close is *marked* closed in its ledger, with the
  section and paragraph it landed in named. A reconciliation that cannot say
  where it went did not happen.
- **Mark the ledger, not just the doc.** Every deviation entry carries a
  `**Status:**` line: `OPEN` until a step reconciles it, then the
  `**RECONCILED**` / `**RESOLVED**` / `**FIXED**` marker `ops/DEVIATIONS.md`
  and `ops/gcc/DEVIATIONS.md` already use in their prose.
  `ops/unicode-operators/clang/DEVIATIONS.md` has never marked a row and its
  24 entries are all `OPEN`; it uses the same field and the same words.
- **Decisions are the author's.** A `Decide` step produces options, costs and
  a recommendation. It does not choose. It ends by asking, and the next step
  is blocked until it is answered — that is a legitimate BLOCKED handoff, not
  a failure.
- **Everything new is named, not numbered.** Steps, decisions, questions and
  defects get slugs — short kebab-case names for the question or the job,
  never for the answer, so a slug survives its own conclusion reversing. The
  ordinals in the checklist are reading order only; cross-reference by slug.
  See `~/.claude/CLAUDE.md`, "Name things for what they are, not what number
  they came in at", and [slug-the-ledgers](steps/slug-the-ledgers.md), which
  retired the numbers this repo used to have.

  This is now a rule for **anything a step adds**, not only for what that step
  renamed. A new decision, a new deviation row, a new backlog entry: give it a
  section headed by its slug in the ledger that owns it, so the slug is a
  Markdown anchor, and make every reference to it a **link** to that anchor —
  links can be followed, and a rename becomes a detectable break instead of a
  silent one. Add the pair to [`ops/SLUGS.md`](../SLUGS.md) only if you are
  retiring a number; a slug that never had one needs no row there.

Commit prefixes: `[backtick] <slug>: …` / `[unicode] <slug>: …` on the feature
branches, `docs: …` for design-doc and paper edits, `ops: <slug> — …` for
bookkeeping here.

## What this plan absorbs

`ops/backlog/PLAN.md` is **superseded, not deleted.** Its BL01–BL04 are green
and their handoffs are the record. Its three unstarted steps have good step
files, and this plan reuses them rather than restating them:

| Old | New home |
|---|---|
| `BL05` ([`increment-decrement-mangling`](../BACKLOG.md#increment-decrement-mangling)) | **upstream-reports** — executes `steps/BL05-upstream-mangling.md` unchanged |
| `BL06` ([c-mode-tokenization](../BACKLOG.md#c-mode-tokenization) [backtick-source-range](../BACKLOG.md#backtick-source-range) [dead-nesting-diagnostic](../BACKLOG.md#dead-nesting-diagnostic) [template-ast-print-test](../BACKLOG.md#template-ast-print-test) [libclang-cursor-arm](../BACKLOG.md#libclang-cursor-arm)) | split by purpose: [c-mode-tokenization](../BACKLOG.md#c-mode-tokenization) [backtick-source-range](../BACKLOG.md#backtick-source-range) → **clang-paper-truth**, [`template-ast-print-test`](../BACKLOG.md#template-ast-print-test) → **gcc-resync**, [dead-nesting-diagnostic](../BACKLOG.md#dead-nesting-diagnostic) [libclang-cursor-arm](../BACKLOG.md#libclang-cursor-arm) → **reconcile-remainder** |
| `BL07` ([lldb-hunk-verification](../BACKLOG.md#lldb-hunk-verification) [code-completion-priority](../BACKLOG.md#code-completion-priority) [ucd-input-manifest](../BACKLOG.md#ucd-input-manifest)) | **gcc-resync**, whose step file supersedes `BL07`'s |
| `M2` | unchanged, still in `ops/unicode-operators/clang/PLAN.md` Phase G; runs after **clang-paper-truth** |

`BL06`'s five rows are the clearest case of the old ordering: they were one
step because they sit on one branch, and they are five unrelated jobs — a
correctness bug that makes C accept C++ grammar, a source-range defect, dead
code, a missing test, and a `-Wswitch` warning. Batching by branch is a real
economy and this plan keeps it *within* a purpose, not across purposes.

## Branches, worktrees, build dirs

Unchanged from `ops/backlog/PLAN.md`, plus one:

| Track | Branch | Worktree | Build dir |
|---|---|---|---|
| backtick | `backtick-trunk` | `~/src/llvm/backtick-trunk` | `~/src/llvm/build-backtick-trunk` |
| backtick | `backtick-23` | `~/src/llvm/backtick` | `~/src/llvm/build-backtick` |
| unicode | `unicode-operators-experiment` | `~/src/llvm/unicode` | `~/src/llvm/build-unicode` |
| unicode | `unicode-operators-upstream` | `~/src/llvm/unicode-upstream` | `~/src/llvm/build-unicode-upstream` |
| GCC | `backtick` | `~/bld/gcc/gcc-backtick` | `~/bld/gcc/gcc-backtick-build` |
| *scratch* | — | `~/src/llvm/unicode` | `~/src/llvm/build-cir-scratch` (MLIR + CIR, **standing**) |

`~/src/llvm/main` / `~/src/llvm/build-main` is the maintainer's pristine pair.
Do not build in it. *Running* its binaries is fine and is how BL04 proved the
`DirectoryWatcherTest` failures were environmental.

**The CIR scratch dir survives BL04 and is reusable.** gcc-resync needs lldb; add
`lldb` to that configure rather than standing up a second tree.

## Gate facts

All of `ops/backlog/PLAN.md`'s "Gate facts" section still applies and is not
restated here — read it. Two updates:

- **[`inotify-watch-budget`](../BACKLOG.md#inotify-watch-budget) is reopened and is an environment condition, not a defect to fix.**
  `cloud-drive-dae` is the machine's continuous backup, so watching every file
  is its job and its hoard tracks the tree size; it grew from ~65k to 523,774
  of the raised 524288. 524288 is a better ceiling than the 65536 default, not
  a guarantee. The 8 `DirectoryWatcherTest.*` cases are the only thing in
  `check-clang` competing for free watches. **Never filter them, never budget
  them as expected failures** — and a gate whose *only* failures are those 8 is
  an environment reading. Confirm with the pristine `build-main` binary.
- **Enabling CIR moves the counts by more than the CIR directory**: +44
  discovered (`CIRUnitTests`), −888 unsupported, +932 passed, the last term
  including `Frontend/cir-not-built.c` going the other way. BL04's handoff has
  the full arithmetic.
- **`Parser::isFoldOperator`'s excluded-level clause is a standing guard on all
  four Clang branches, and losing it fails silently.** **The clause is spelled
  differently on each pair, which is itself a trap**: it is
  `Level != prec::Backtick` on `backtick-trunk` and `backtick-23` (where the
  level is `prec::Backtick = 16`) and `Level != prec::UserInfix` on
  `unicode-operators-experiment` and `unicode-operators-upstream`. Grepping all
  four for `prec::UserInfix` — which is what this bullet used to say — comes
  back **empty on the backtick branches and looks exactly like the loss it
  warns about** (corrected 2026-09-06 by
  [clang-paper-truth](steps/clang-paper-truth.md), which checked all four). Dropping
  it admits a user-introduced infix operator as a fold operator — no
  diagnostic, no test failure unless the negative test is present, and a
  behaviour change to `(... ⊞ N)` and `` (... `f` N) ``, both of which are
  deliberately ill-formed
  ([fold-over-user-infix](../../docs/open-decisions.md#fold-over-user-infix),
  answered 2026-09-06). **A replay onto clean `main` must *add* the clause,
  not rename one** — on `main` the predicate ends at `prec::Spaceship`.
  Check it after every rebase and every replay, on `backtick-trunk`,
  `backtick-23`, `unicode-operators-experiment` and
  `unicode-operators-upstream`; `REPLAY.md`'s `U11` row calls it the single
  likeliest replay mistake in that step. The negative tests that pin it are
  `clang/test/Parser/unicode-operator-precedence.cpp` section 9 and its
  backtick twin — do not delete them as redundant.

## Baselines

`ops/backlog/PLAN.md`'s table, as BL04 updated it: `backtick-trunk`
54108/48222/**0**, `backtick-23` 54342/48500/**1** ([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)),
`unicode-operators-experiment` 54183/48295/**0**,
`unicode-operators-upstream` 54241/48323/**0**; XFAIL 27 and skipped 6
throughout. Steps that add a test update that table in *this* file's Status
log and leave the old plan's alone.

**Updated 2026-09-06 by [clang-paper-truth](steps/clang-paper-truth.md)**, which
added one test file (`clang/test/Lexer/backtick-c-mode.c`) to both backtick
branches: `backtick-trunk` **54109/48223/0**, `backtick-23`
**54343/48501/1** ([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)).
The two Unicode rows are unchanged and untouched. Unsupported stays at 5853 /
5808 — the new file is a lit test, not a `clang/test/CIR/` one, so it lands in
*passed* rather than in *unsupported*, unlike BL04's additions.

**Updated again 2026-09-06 by [evidence-debt](steps/evidence-debt.md)**, which
added exactly one lit test file to **each** of the four branches — the
template `-ast-print` test to the two backtick branches, the completion-priority
test to the two Unicode ones. Every row moves by **+1 discovered / +1 passed**
and by nothing else:

| Branch | Discovered | Passed | Failed |
|---|---|---|---|
| `backtick-trunk` | 54110 | 48224 | **0** |
| `backtick-23` | 54344 | 48502 | **1** ([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)) |
| `unicode-operators-experiment` | 54184 | 48296 | **0** |
| `unicode-operators-upstream` | 54242 | 48324 | **0** |

XFAIL 27 and skipped 6 throughout; unsupported unchanged at 5853 / 5808 / 5855
/ 5885. All four measured unfiltered with `ulimit -c 0` and the exit code
captured explicitly.

**Updated again 2026-09-06 by [clang-slot-adl](steps/clang-slot-adl.md)**, which
added one lit test file (`clang/test/SemaCXX/backtick-adl.cpp`) to the two
**backtick** branches and touched neither Unicode branch. Both move by **+1
discovered / +1 passed** and by nothing else; unsupported is unchanged, so the
new file lands in *passed*:

| Branch | Discovered | Passed | Failed |
|---|---|---|---|
| `backtick-trunk` | 54111 | 48225 | **0** |
| `backtick-23` | 54345 | 48503 | **1** ([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)) |
| `unicode-operators-experiment` | 54184 | 48296 | **0** *(untouched)* |
| `unicode-operators-upstream` | 54242 | 48324 | **0** *(untouched)* |

XFAIL 27, skipped 6, unsupported 5853 / 5808 / 5855 / 5885 — all unchanged.

**Updated again 2026-09-07 by [hygiene-parity](steps/hygiene-parity.md)**, which
added **three gtest cases** — two in `clang/unittests/ASTMatchers/ASTMatchersNodeTest.cpp`
and one in `clang/unittests/Format/FormatTest.cpp` — to the two **backtick**
branches and touched neither Unicode branch. Both move by **+3 discovered / +3
passed** and by nothing else; unsupported is unchanged, so all three land in
*passed*. **gtest cases are individually discovered lit tests**, which is why a
step that adds no `.cpp` file under `clang/test/` still moves these numbers:

| Branch | Discovered | Passed | Failed |
|---|---|---|---|
| `backtick-trunk` | 54114 | 48228 | **0** |
| `backtick-23` | 54348 | 48506 | **1** ([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)) |
| `unicode-operators-experiment` | 54184 | 48296 | **0** *(untouched)* |
| `unicode-operators-upstream` | 54242 | 48324 | **0** *(untouched)* |

XFAIL 27, skipped 6, unsupported 5853 / 5808 / 5855 / 5885 — all unchanged.
Both backtick rows measured unfiltered with `ulimit -c 0` and the exit code read
from the log rather than through a pipe.

**Updated again 2026-09-07 by [unicode-branch-maintenance](handoffs/unicode-branch-maintenance.handoff.md)**,
which forward-ported the three completion steps' backtick work to
`unicode-operators-experiment` in one merge. That branch gains **two lit test
files** (`SemaCXX/backtick-adl.cpp`, `AST/backtick-template-print.cpp`) and
**three gtest cases** (two in `ASTMatchersNodeTest.cpp`, one in
`FormatTest.cpp`), so it moves by **+5 discovered / +5 passed** and by nothing
else. `unicode-operators-upstream` took a comment-only commit and does not move
at all. The two backtick rows were not touched:

| Branch | Discovered | Passed | Failed |
|---|---|---|---|
| `backtick-trunk` | 54114 | 48228 | **0** *(untouched)* |
| `backtick-23` | 54348 | 48506 | **1** ([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)) *(untouched)* |
| `unicode-operators-experiment` | **54189** | **48301** | **0** |
| `unicode-operators-upstream` | **54242** | **48324** | **0** |

XFAIL 27, skipped 6, unsupported 5853 / 5808 / 5855 / 5885 — all unchanged.
**Both Unicode gates needed two runs**, and the first run of each failed a
*different* three `DirectoryWatcherTest.*` cases: that is
[`inotify-watch-budget`](../BACKLOG.md#inotify-watch-budget) at its worst
recorded level — `cloud-drive-dae` holding **523,730 of 524,288** watches, at
which point all **eight** cases fail on the pristine `~/src/llvm/build-main`
binary. Confirmed by cause and by control, **not filtered and not budgeted**;
the numbers above are the clean re-runs.

**Unchanged 2026-09-07 by [settle-paper-rows](steps/settle-paper-rows.md)**,
recorded because a step that touches two branches and moves no number is worth
saying so about: its two cases went into lit files that already existed
(`clang/test/Parser/backtick-infix.cpp`, `clang/test/Parser/backtick-ast-print.cpp`),
and lit discovers files, not cases. `backtick-trunk` **54114 / 48228 / 0** and
`backtick-23` **54348 / 48506 / 1**
([`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config)),
both measured unfiltered with the exit code read from the log; XFAIL 27,
skipped 6, unsupported 5853 / 5808 unchanged. Neither Unicode branch was
touched. **The GCC number does move**: `dg.exp=g++.dg/backtick/*.C` goes from
**103** expected passes to **109**, because dejagnu counts directives and the
two added to `g++.dg/backtick/escape-diag.C` run at three standards.

## Checklist

Each line carries an **ordinal and a slug**. The ordinal is reading order and
shifts whenever a step is inserted or split; **the slug is the identity, and
every cross-reference in this repo uses it.** Never write "step 7".

Three box states: `[ ]` unstarted, `[x]` green and gated, and **`[—]`
not-applicable** — a step whose scope turned out to be empty, with the reason
on the line. `[—]` is deliberately not `[ ]`: an agent looking for "the first
unchecked step whose dependencies are checked" must skip it rather than go
hunting for work in it, and it is deliberately not `[x]` either, because
nothing was done and no gate was run.

### Phase A — Naming (first, so nothing downstream is written twice)
- [x] 1. [slug-the-ledgers](steps/slug-the-ledgers.md) — retire the serial numbers in the two decision logs, the three deviation ledgers and the backlog in favour of slugs; [`ops/SLUGS.md`](../SLUGS.md) is the map (dep: none)

### Phase B — Upstream citizenship (no dependencies; early because issues take calendar time)
- [x] 2. [upstream-reports](steps/upstream-reports.md) — file the three upstream defects (dep: none) — **green as amended, 2026-09-06. The author decided the step drafts the reports and does not file them**, so its "three issue URLs" gate bullet was removed by the person who owns the plan rather than failed by the agent; everything else in the gate passed. The three reports are written out ready to post in [`upstream-drafts/`](upstream-drafts/README.md). **The three backlog rows stay open** — each `Closed by` says "drafted, pending the maintainer filing it" — and there is no issue number for anything to cite. Ticked so the next agent does not redo the confirmation work; do not read it as "the defects are reported".
- [x] 3. [upstream-triage](steps/upstream-triage.md) — report-or-WONTFIX the five annoyances (dep: none) — **green on its own gate, 2026-09-06, with the same amendment as upstream-reports: draft, do not file.** All five rows are triaged and have reasoned `Closed by` cells. **Two genuinely close** — [`inner-call-source-range`](../BACKLOG.md#inner-call-source-range) and [`operator-caret-range`](../BACKLOG.md#operator-caret-range), both WONTFIX with the reason recorded in the row *and* in a design doc, so the papers answer the question rather than being asked it. **Three stay open pending the maintainer filing them**: [`cxxfilt-stdin-nonascii`](../BACKLOG.md#cxxfilt-stdin-nonascii), [`auto-return-round-trip`](../BACKLOG.md#auto-return-round-trip), [`pch-ast-print-order`](../BACKLOG.md#pch-ast-print-order), drafted in [`upstream-drafts/`](upstream-drafts/README.md). **The sixth item this step was given — the report half of [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id) — was NOT done and has no draft**, because the literal-operator reproducer it was to be filed against is *correctly* rejected and the report would be wrong; the question is reopened for the author in [`docs/open-decisions.md`](../../docs/open-decisions.md#2026-09-06--dependent-template-operator-id-the-report-half-is-reopened). Do not read this tick as "the six are reported".

### Phase C — Decide (the author's, and the critical path for everything written)
- [x] 4. [decision-brief](steps/decision-brief.md) — the four questions with implementation consequences, and the "anywhere" claim (dep: none) — brief at [`docs/open-decisions.md`](../../docs/open-decisions.md); **answered 2026-09-06, all five recommendations accepted, and the answers are recorded there.** Nothing turned into code: see implement-decisions below.
- [x] 5. [mangling-abi](steps/mangling-abi.md) — the ABI question, and U§9 with it (dep: upstream-reports ✔ *as amended*, slug-the-ledgers ✔) — **unblocked, but the thing it wanted upstream-reports for does not exist**: the `pp_`/`pp` report is drafted and unfiled, so there is no issue number to cite. Write around the placeholder token `LLVM-ISSUE-PENDING`; do not invent a number and do not wait for one. — **U§9 is written and two of its three parts are settled; the third is BLOCKED on the author, 2026-09-06.** [mangling-derivation-rule](../../docs/unicode-operators.md#mangling-derivation-rule) and [microsoft-abi-position](../../docs/unicode-operators.md#microsoft-abi-position) reconcile [`vendor-extended-mangling`](../unicode-operators/clang/DEVIATIONS.md#vendor-extended-mangling) and [`msvc-mangling`](../unicode-operators/clang/DEVIATIONS.md#msvc-mangling) and close [`astral-plane-mangling`](../BACKLOG.md#astral-plane-mangling); [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators)'s mangling clause is taken. **Answered 2026-09-06 — the recommendation accepted as written**, so the box is ticked: [abi-production-request](../../docs/unicode-operators.md#abi-production-request) now records *(a) and (b) together, non-normatively* — the vendor form as the fallback that needs no ABI action, plus a first-class production asked for as a **request**, with `s` reserved so postfix stays takeable. Same shape as [decision-brief](steps/decision-brief.md): BLOCKED on the author from writing to answer, then green.

### Phase D — Make the papers true
- [x] 6. [clang-paper-truth](steps/clang-paper-truth.md) — the three Clang defects that falsify a claim (dep: decision-brief ✔ — answered, not blocked) — **green on both backtick branches, 2026-09-06.** All three rows closed. Two of the three were **wider than the step file scoped them**: [`backtick-source-range`](../BACKLOG.md#backtick-source-range) needed the type slot as well as the call, because `BL02` post-dates `BL06`; [`keyword-escape-round-trip`](../BACKLOG.md#keyword-escape-round-trip) took six sites, not one, and turned up an upstream split in which *diagnostic argument kind* decides whether a name is escaped. The diagnostic-wording decision the step file demanded is [keyword-escape-printing](../../docs/backtick-operator-design.md#keyword-escape-printing) — **taken by the step, not by the author**, because the fix cannot land without it; the entry states the reversal cost and nothing depends on the answer. **M2 is now runnable**; the handoff has three specific conflict warnings for it.
- [x] 7. [clang-slot-adl](steps/clang-slot-adl.md) — Clang does no ADL on the backtick slot (dep: none) — **inserted 2026-09-06, after [evidence-debt](steps/evidence-debt.md) found the defect and this plan had already been written.** **Green on both backtick branches.** Fixed rather than reworded, which is the author's decision: the slot's bare unqualified name — plain identifier *and* template-id, in one pass — now reaches `BuildCallExpr` unresolved, so `` u `pick` u `` and `pick(u, u)` bind the same function again. §17.4's status-correction block is gone and the section carries the finding [backtick-paper](steps/backtick-paper.md) is to write from: **a within-compiler control**, Unicode right and backtick wrong in one build, which is stronger evidence for *inherit, don't reimplement* than the cross-compiler note beside it, plus the near-miss — nine steps missed this because the track's one "ADL" test used a *qualified* name.
- [x] 8. [null-return-suppression](steps/null-return-suppression.md) — the analyzer parity break, on all four branches (dep: none) — **green on all four, 2026-09-06.** **The row's stated mechanism was wrong and the fix is not where it said**: peeling at `CallEvent::isCallStmt` changes nothing, because that handler is never reached — `Tracker::track`'s `findNodeForExpression` finds no graph node for a wrapper (the CFG has no element for one, which is what BL03's six arms arranged) and abandons the whole tracking chain. One arm per wrapper in `peelOffOuterExpr` instead. **The defect was also bigger than recorded in a second direction**: the report that *was* emitted carried 2 path notes against the explicit call's 8, so the operator form was noisier *and* less explained. `bugs_are_still_found` rewritten and both Analysis tests given a second `-verify` RUN line at the default suppression setting. Reconciled into `docs/backtick-operator-design.md` **§17.6** (new) and `docs/unicode-operators.md` **§8**.
- [—] 9. [implement-decisions](steps/implement-decisions.md) — build whatever decision-brief decided (dep: decision-brief; scope contingent) — **NOT APPLICABLE: decision-brief was answered 2026-09-06 and all five answers are "keep what is built and argue for it", so this step has an empty scope.** Not ticked, per its own step file's "If the answer was 'no change'"; the documentation those answers generate belongs to [reconcile-declaring-using](steps/reconcile-declaring-using.md), [reconcile-remainder](steps/reconcile-remainder.md) and [upstream-triage](steps/upstream-triage.md).
- [x] 10. [gcc-resync](steps/gcc-resync.md) — re-sync GCC to current trunk, then its four open defects (dep: none)

### Phase E — Discharge the evidence debt
- [x] 11. [evidence-debt](steps/evidence-debt.md) — the unbuilt hunk, the unwritten test, the unregenerable table (dep: none; supersedes `BL07`) — **green on all four branches, 2026-09-06. All four rows closed, and one of them turned up a P1 nobody had.** The lldb hunk compiles on both Unicode branches (two trees, because their bases differ); [`code-completion-priority`](../BACKLOG.md#code-completion-priority) is fixed and tested — **its row's stated observable was wrong**, the printing consumer sorts by *name* and never prints priority, so the test asserts `c-index-test`'s printed number instead; the UCD manifest exists and the tables **regenerate byte-identically**; and the template `-ast-print` test is written and proven to catch what the old one could not. **[clang-slot-adl](../BACKLOG.md#clang-slot-adl) is new and unowned**: writing the template test showed that Clang does no ADL on the backtick slot at all, so `` u `pick` u `` and `pick(u, u)` can silently call different functions — [§17.4](../../docs/backtick-operator-design.md#174-adl-is-normative-cross-compiler-note) is normative, reports the rule delivered, and is false. §17.4 carries a dated status-correction block pointing at the ledger; the fix-or-reword choice is the author's.

### Phase F — Reconcile, one destination section per step
- [x] 12. [reconcile-implementation-cost](steps/reconcile-implementation-cost.md) — U§8, the implementation-cost thesis (dep: evidence-debt ✔, slug-the-ledgers ✔) — **green on the docs gate, 2026-09-06. All eight rows marked, and [`matcher-operator-name`](../BACKLOG.md#matcher-operator-name) closed as designed.** U§8's three Clang bullets are now eight, with the parser split into *declaring* and *using* per [`declaring-side-parse-cost`](../unicode-operators/clang/DEVIATIONS.md#declaring-side-parse-cost). Two new slug-headed subsections carry the findings the bullets cannot: [dispatch-obligation-taxonomy](../../docs/unicode-operators.md#dispatch-obligation-taxonomy) and [closed-table-sibling-pattern](../../docs/unicode-operators.md#closed-table-sibling-pattern). **Three of the four counts this step was told to quote were wrong and every one was found by re-measuring rather than by reading**: the `NameKind` axis is **34 sites in 17 files**, not 33 in 20; the declaring side is **12 `UnqualifiedIdKind` sites in 5 files** of which only 7 are compiler-found, not "9, all compiler-found"; and the node's dispatch fan-out is **43 sites in 35 files**, not 32 in 21 — that figure was one addition short of its own inputs when it was written. The original 6/8/1/13 = 28 accounting reproduces site for site and was right for the tree it was taken on; what moved was the tree. **Two rows are marked partly closed and say so**: [`operator-candidate-assembly`](../unicode-operators/clang/DEVIATIONS.md#operator-candidate-assembly)'s U§7 half is [reconcile-declaring-using](steps/reconcile-declaring-using.md)'s and [`ast-node-shape`](../unicode-operators/clang/DEVIATIONS.md#ast-node-shape)'s one U§12 sentence is [reconcile-remainder](steps/reconcile-remainder.md)'s. **U§6's contested sentence was not touched**, per the step file; the U§8-side evidence and the caveat are in U§8's opening paragraph and the handoff hands the sentence over.
- [x] 13. [reconcile-declaring-using](steps/reconcile-declaring-using.md) — U§7 / §7.1, declaring, using, desugaring (dep: decision-brief ✔, implement-decisions **[—] n/a — satisfied**, slug-the-ledgers ✔) — **green on the docs gate, 2026-09-06. Seven rows marked, plus the U§7 half of [`operator-candidate-assembly`](../unicode-operators/clang/DEVIATIONS.md#operator-candidate-assembly), and [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id) closed.** U§7 "Declaring" enumerates the five [over.oper] restrictions as a table and states the generalization as a rule; the default-argument waiver and its cross-fixity consequence are written down instead of being reached by omission; U§7 "Using" is three bullets (one candidate set ranked together, ADL inherited by not writing code, no built-in candidates as a *non-mechanism*); U§7 "Desugaring" softens *"without modification"* and states the two-case sequencing rule, asking the member/non-member question of CWG rather than inheriting it by silence; U§7.1 no longer says **"anywhere"** and carries the dependent-template exception with the accurate reason. The owed decision entry exists: [static-member-operators](../../docs/unicode-operators.md#static-member-operators). **Two falsifiable sentences were found already in `papers/unicode-mathematical-operators.md`** — the struck literal-operator clause and the abandoned static-member reason — and both are corrected there in the same commit. **One more recorded mechanism did not survive checking:** `Sema::ActOnUserOperator`, not `CreateOverloadedUserOp`, performs the operator-name lookup.
- [x] 14. [reconcile-remainder](steps/reconcile-remainder.md) — U§5 / §10 / §6 / §12 / §13, and the backtick and GCC ledgers (dep: gcc-resync ✔, slug-the-ledgers ✔) — **green on the docs gate, 2026-09-06. All three deviation ledgers now have zero unreconciled rows, for the first time since the tracks began.** Eleven rows marked (six Unicode + [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators), which its step file did not list but its own gate required, + four backtick), plus the three left partly closed by earlier steps ([`infix-parse-cost`](../unicode-operators/clang/DEVIATIONS.md#infix-parse-cost), [`prefix-arity-selection`](../unicode-operators/clang/DEVIATIONS.md#prefix-arity-selection), [`ast-node-shape`](../unicode-operators/clang/DEVIATIONS.md#ast-node-shape)); [`gcc-type-slot-parity`](../gcc/DEVIATIONS.md#gcc-type-slot-parity) was already taken by gcc-resync. [`confusable-spellings`](../BACKLOG.md#confusable-spellings) closed as **curated, with the principle written down**, in the new decision entry [confusable-spelling-provenance](../../docs/unicode-operators.md#confusable-spelling-provenance). **U§6's contested sentence is rewritten and the correction is reported as a finding**; U§6 gains the missing sixth worked example and the structural one-liner; U§12 states separable fates as an executed result; U§13 gains its fold bullet; the backtick doc gains **§17.7** (the two features converge in the back end) and **§17.8** (which of the wrapper's cost has no GCC counterpart *by construction*). **Two more recorded claims failed re-checking, making five mechanisms across five steps**: [`ucn-operator-spellings`](../unicode-operators/clang/DEVIATIONS.md#ucn-operator-spellings)'s clause (c) is wrong — the exclusion diagnostics *do* fire for UCN spellings — and [`replay-ordering`](../unicode-operators/clang/DEVIATIONS.md#replay-ordering)'s headline grep is not reproducible without its base commit pinned. A fourth bad count too: clang-format is **+40/−1** standalone, not +32/−9. **Both papers carried claims the answers had discarded** — the Unicode paper called the fold exclusion *inherited* and *an open question*, and quoted the wrong clang-format figure — and are corrected; the backtick paper gained its owed fold sentence. **`docs/unicode-operators.md` had silently lost its `## 11. Prior art` heading** to an earlier edit, which is why several ledger rows cite a `U§11` that no longer existed; restored.

### Phase G — Hygiene (no paper consequence; any time after its branches settle)
- [x] 15. [hygiene-parity](steps/hygiene-parity.md) — the tooling-parity gaps, the dead code, the formatting limit (dep: clang-paper-truth) — **green on both backtick branches, 2026-09-07.** All four rows fixed and [`template-id-code-point`](../BACKLOG.md#template-id-code-point) recorded. Two of the five had a recorded fact that measurement corrected. **[`slot-split-penalty`](../BACKLOG.md#slot-split-penalty) got both of the step's two alternatives**: the bump is implemented *and* the limit behind it is pinned, because implementing it is what located the limit — it decides ties (25 changed outputs over a 10-shape sweep) and cannot beat `PenaltyExcessCharacter`, so a long slot with no alternative break still splits, and a bump big enough to stop that would be the no-break zone [format-break-policy](../../docs/backtick-operator-design.md#format-break-policy) rejects. **[`template-id-code-point`](../BACKLOG.md#template-id-code-point) is cheaper than its own `FIXME` claims** — measured, its diagnostics are character-identical to `operator""_sfx`'s on a stock compiler, so there is not even the diagnostic-quality cost. And the [`libclang-cursor-arm`](../BACKLOG.md#libclang-cursor-arm) `-Wswitch` warning had been live for **ten weeks**, across every step of two tracks including the one that recorded it, which is the half-life datum [`expression-node-cost`](../unicode-operators/clang/DEVIATIONS.md#expression-node-cost)'s taxonomy wanted. **No paper was edited, and that was checked rather than assumed.**

### Phase H — The papers
- [x] 16. [backtick-paper](steps/backtick-paper.md) — D4307R0 and its blog version (dep: clang-paper-truth, [clang-slot-adl](steps/clang-slot-adl.md) ✔, reconcile-remainder, hygiene-parity) — **done 2026-09-07. 57 implementation claims re-derived against the built compilers; 5 failed, recorded as 4 rows.** The ADL section is rewritten around what actually happened — both compilers wrong, the same way — and carries the within-compiler control and the augmentation shape. **The paper could not produce a PDF and nobody had noticed**: a `header-includes` key in its front matter replaced the wg21 LaTeX preamble, so `\pnum` was undefined; the style block moved into the body and both formats now build. Four new rows opened, none of them owned: [`escape-name-positions`](../DEVIATIONS.md#escape-name-positions) and [`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape), [`escape-alias-name-parity`](../gcc/DEVIATIONS.md#escape-alias-name-parity) and [`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling). §17.8's *"the one place the two implementations genuinely disagree"* is now two, and §17.5's *three shapes* is four.
- [x] 17. [unicode-paper](steps/unicode-paper.md) — the Unicode paper, a real number, and its blog version (dep: mangling-abi, implement-decisions **[—] n/a — satisfied**, reconcile-implementation-cost, reconcile-declaring-using, reconcile-remainder) — **done 2026-09-07. The author assigned D4345R0, *Extending C++ with Unicode Mathematical Operators*, and both papers were renamed by name in the same sitting.** `papers/unicode-mathematical-operators.md` and `papers/backtick-infix-and-keyword-escape.md`; **not** `d4345r0.md` and `d4307r0.md`, because the convention names a paper for what it is and puts the number in the front matter, and renaming a placeholder number to a real one would have entrenched the thing the convention forbids. **89 claims re-derived against the built compilers; 14 failed and all 14 are corrected.** The confusable exclusion list omitted U+2044, which *is* on the shipped table; the volume, commit, ledger-row and step counts were all stale because the branch kept growing after the figures were taken; the 67-site dispatch table was a mixed-axis count that no longer reproduces and is now 89 over three axes, with the node's 43 given [dispatch-obligation-taxonomy](../../docs/unicode-operators.md#dispatch-obligation-taxonomy)'s seven categories, dated, branch-stamped and summing; *three-for-three* is five; and the candidate assembler's split is a clean operator-kind/name line rather than one six-line helper. **The paper's PDF could not show its own subject matter** — Latin Modern Mono carries none of the operator glyphs, so 136 characters were missing over 14 pages and every code block rendered `operator` followed by nothing, with the HTML target silent about it; a `monofont` in the front matter and naming the twelve emoji by code point takes it to zero. U§9's answered ABI section is carried as the author ruled: the vendor form as the fallback needing no ABI action, then the first-class production **as a request**, letters left to the ABI group, postfix's slot reserved. *What is not resolved* now lists only the four things somebody else must answer; the four the author decided are argued in the body. `docs/unicode-infix-operators.org` said *nothing is implemented*, false since the prototype was built. **This step opened no ledger row and the Unicode ledger is still clear**; the four rows [backtick-paper](steps/backtick-paper.md) opened remain backtick-track and unowned, and the Coverage table now says so on both halves. See [unicode-paper](handoffs/unicode-paper.handoff.md).

### Phase I — The rows the last pass opened (after the papers, because they came from one)
- [ ] 18. [settle-paper-rows](steps/settle-paper-rows.md) — the four rows [backtick-paper](steps/backtick-paper.md) opened, and who owns each (dep: backtick-paper ✔) — **inserted 2026-09-07, after the plan was complete, for the same reason [clang-slot-adl](steps/clang-slot-adl.md) was: a defect the plan could not have known about, found by a step it did contain.** Two are settled, two are the author's. **[`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape) fixed** on both backtick branches — the fourth inner shape a type slot can build, and pre-fix the deduced case printed a *different program*, not merely the desugaring. **[`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling) fixed** in GCC, so the ratified printing ruling now has two-compiler evidence for the half both compilers have; the fix was wrong on its first build in exactly the way the branch's previous commit warns about, and the second one is the finding. **[`escape-name-positions`](../DEVIATIONS.md#escape-name-positions) and [`escape-alias-name-parity`](../gcc/DEVIATIONS.md#escape-alias-name-parity) are one question and are BLOCKED on the author**, priced in both compilers as the sixth page of [`docs/open-decisions.md`](../../docs/open-decisions.md#escape-name-positions). Nineteen positions were probed in each compiler and **the boundary turned out never to have been decided by anyone** — it fell where each parser happens to route its names, which is also why the divergence is three programs and not one. **The box stays unchecked until that is answered**, per the ground rule that a `Decide` step ends by asking.

### Maintenance (not plan steps)
- **M2 — done, 2026-09-06.** Forward-ported `BL02` + clang-paper-truth's
  backtick fixes to `unicode-operators-experiment`; gate green against the
  Baselines row, which is unchanged because the merge added no test file that
  branch did not already have. The predicted BL04 lead-comment conflict happened
  four times and was exactly as trivial as predicted; the eight conflicts and
  their resolutions are in [M2-forward-port](handoffs/M2-forward-port.handoff.md).
  `unicode-operators-upstream` was **not** touched and must never receive the merge.
- **[unicode-branch-maintenance](handoffs/unicode-branch-maintenance.handoff.md) — done, 2026-09-07.**
  Forward-ported the four commits `backtick-trunk` had gained since M2 —
  [null-return-suppression](steps/null-return-suppression.md)'s backtick half,
  [evidence-debt](steps/evidence-debt.md)'s template `-ast-print` test,
  [clang-slot-adl](steps/clang-slot-adl.md) and
  [hygiene-parity](steps/hygiene-parity.md) — to `unicode-operators-experiment`
  in one merge, and carried M2's `CheckUserOperatorDeclaration` comment fix to
  `unicode-operators-upstream`, which closes the one-paragraph divergence M2
  left between the two Unicode branches. **Seven conflicts, none semantic**;
  four of them are `U17`'s Unicode node and hygiene-parity's backtick node
  wanting the same anchor, resolved *keep both, backtick after Unicode*, and
  two are test blocks sharing a closing brace. Both branches gate green. The
  merge goes to the experiment branch **only** and must never reach
  `unicode-operators-upstream`.
- **[inotify-watch-budget](../BACKLOG.md#inotify-watch-budget)** — needs root, and is the maintainer's. Not an agent step.
- **Housekeeping — done, 2026-09-06.** `CLAUDE.md`'s Layout section named
  neither `docs/unicode-operators.md` nor `docs/open-decisions.md`;
  [mangling-abi](steps/mangling-abi.md) added the second and extended the
  first's ABI clause, having declined to leave it to a fourth step.

## The critical path, and what is parallel

```
slug-the-ledgers ──> mangling-abi and every reconcile-*

upstream-reports ──> mangling-abi ──────────────────────────────┐
decision-brief ─┬─> implement-decisions ─> reconcile-declaring-using ─┐         │
                └─> clang-paper-truth ─> hygiene-parity ─┐           ├──> unicode-paper
gcc-resync ───────> reconcile-remainder ────────────────┼───────────┘
                                                        └──> backtick-paper
evidence-debt ─┬──> reconcile-implementation-cost ──────────────────────┘
               └──> clang-slot-adl ──> backtick-paper

upstream-triage, null-return-suppression  — independent of everything
```

**clang-slot-adl is drawn out of evidence-debt but does not depend on it** —
it depends on nothing, and would have been schedulable from day one had anyone
run the measurement. The arrow is provenance, not a gate.

**slug-the-ledgers goes first** because every step after it edits documents
dense with `BNN` and `DEV-NN` references; renaming afterwards means touching
the same paragraphs twice, and the second pass is the one that gets skipped.
**decision-brief is the one thing that blocks the most**, and it is desk work: seven
questions that are already measured, needing options and a recommendation.
Start there if only one agent is available. **gcc-resync was the most perishable** and is
done — the branch is on trunk `4df5e1e9b152` (2026-09-06). **upstream-triage and null-return-suppression are independent of
everything** and are the right work for a spare agent.

## Coverage — every open item has a home

31 open `BNN` rows (37 + [`clangir-lvalue-crash`](../BACKLOG.md#clangir-lvalue-crash), less the 7 closed by `BL01`–`BL04`), **plus one added on 2026-09-06** — [`clang-slot-adl`](../BACKLOG.md#clang-slot-adl), which had no row because nobody had run the measurement:

| Step | Rows |
|---|---|
| upstream-reports | [`increment-decrement-mangling`](../BACKLOG.md#increment-decrement-mangling) [`unqualified-id-union-read`](../BACKLOG.md#unqualified-id-union-read) [`clangir-lvalue-crash`](../BACKLOG.md#clangir-lvalue-crash) |
| upstream-triage | [`inner-call-source-range`](../BACKLOG.md#inner-call-source-range) [`cxxfilt-stdin-nonascii`](../BACKLOG.md#cxxfilt-stdin-nonascii) [`auto-return-round-trip`](../BACKLOG.md#auto-return-round-trip) [`pch-ast-print-order`](../BACKLOG.md#pch-ast-print-order) [`operator-caret-range`](../BACKLOG.md#operator-caret-range) |
| decision-brief | none, as it turns out — it *answers* [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id) and closes none of it. Its real output is five decisions, four of which never had a `BNN` row because they were never defects. |
| reconcile-declaring-using | [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id), U§7.1 reword half — **done 2026-09-06; the row is closed on the reword alone, and the papers were corrected with it** |
| upstream-triage (2nd) | [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id), upstream-report half — **not done, and back with the author.** The literal-operator reproducer it was to be filed against is correctly rejected (a literal operator can never be a class member, [over.literal]/1, and trunk says so at the site), so the report would be wrong and none was drafted. See [the reopening](../../docs/open-decisions.md#2026-09-06--dependent-template-operator-id-the-report-half-is-reopened); the reword half is unaffected and is still reconcile-declaring-using's |
| mangling-abi | [`astral-plane-mangling`](../BACKLOG.md#astral-plane-mangling) |
| clang-paper-truth | [`keyword-escape-round-trip`](../BACKLOG.md#keyword-escape-round-trip) [`c-mode-tokenization`](../BACKLOG.md#c-mode-tokenization) [`backtick-source-range`](../BACKLOG.md#backtick-source-range) |
| null-return-suppression | [`null-return-suppression`](../BACKLOG.md#null-return-suppression) |
| gcc-resync | [`template-id-slot-adl`](../BACKLOG.md#template-id-slot-adl) [`module-streaming-escapes`](../BACKLOG.md#module-streaming-escapes) [`grokdeclarator-guard-scope`](../BACKLOG.md#grokdeclarator-guard-scope) [`gcc-wrapper-parity`](../BACKLOG.md#gcc-wrapper-parity) [`gcc-trunk-pin`](../BACKLOG.md#gcc-trunk-pin) |
| evidence-debt | [`template-ast-print-test`](../BACKLOG.md#template-ast-print-test) [`lldb-hunk-verification`](../BACKLOG.md#lldb-hunk-verification) [`code-completion-priority`](../BACKLOG.md#code-completion-priority) [`ucd-input-manifest`](../BACKLOG.md#ucd-input-manifest) — all four closed 2026-09-06 |
| [clang-slot-adl](steps/clang-slot-adl.md) **(step inserted 2026-09-06)** | [`clang-slot-adl`](../BACKLOG.md#clang-slot-adl) — **closed the same day, and it leaves [backtick-paper](steps/backtick-paper.md) a finding rather than a repair.** Found by evidence-debt, **P1**, and fixed rather than reworded on the author's decision, so [§17.4](../../docs/backtick-operator-design.md#174-adl-is-normative-cross-compiler-note) keeps its normative claim and its status-correction block is gone. The finding to write: **a within-compiler control** — the Unicode feature and the backtick feature in *one* Clang build, the first inheriting ADL and the second not, differing in exactly one thing, whether the slot reaches the call builder unresolved. That is stronger evidence for *inherit, don't reimplement* than the cross-compiler note beside it, because it needs no second implementation. The near-miss goes with it: nine steps missed this because the track's one "ADL" test used a **qualified** name, and the shape that catches it is *augmentation*, the only one whose failure is silent. Evidence in [`clang-slot-adl`](../DEVIATIONS.md#clang-slot-adl) and in [its handoff](handoffs/clang-slot-adl.handoff.md) |
| reconcile-implementation-cost | [`matcher-operator-name`](../BACKLOG.md#matcher-operator-name) (recorded as designed, and as evidence) |
| hygiene-parity | [`backtick-ast-matchers`](../BACKLOG.md#backtick-ast-matchers) [`dead-nesting-diagnostic`](../BACKLOG.md#dead-nesting-diagnostic) [`slot-split-penalty`](../BACKLOG.md#slot-split-penalty) [`libclang-cursor-arm`](../BACKLOG.md#libclang-cursor-arm), and [`template-id-code-point`](../BACKLOG.md#template-id-code-point) recorded. **[`confusable-spellings`](../BACKLOG.md#confusable-spellings) moved to reconcile-remainder and closed there, 2026-09-06** — its own step file claimed it on the grounds that recording a decision is not a hygiene fix, and it was the gate for that step's new decision entry | **All five closed 2026-09-07**, four fixed on both backtick branches and [`template-id-code-point`](../BACKLOG.md#template-id-code-point) recorded as *matches upstream's shape, latent, and measurably cheaper than its own `FIXME` says*.
| slug-the-ledgers | none directly — it renames every row above, and `ops/SLUGS.md` is the map |
| — | [`inotify-watch-budget`](../BACKLOG.md#inotify-watch-budget), maintainer's, needs root |
| [settle-paper-rows](steps/settle-paper-rows.md) **(step inserted 2026-09-07)** | [`escape-name-positions`](../DEVIATIONS.md#escape-name-positions) [`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape) [`escape-alias-name-parity`](../gcc/DEVIATIONS.md#escape-alias-name-parity) [`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling) — four claims the paper made that the built compilers did not support. None of them is a `BNN` row; they are deviation rows and are listed again in the table below. **Two fixed, two with the author** — see the checklist entry |

29 open deviation rows when this plan was written, written out in full so a grep for one finds its step. **Four more were opened on 2026-09-07, after the plan, and have no step** — the last row of this table.

| Step | Rows |
|---|---|
| mangling-abi | [`vendor-extended-mangling`](../unicode-operators/clang/DEVIATIONS.md#vendor-extended-mangling) [`msvc-mangling`](../unicode-operators/clang/DEVIATIONS.md#msvc-mangling) [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators) (mangling clause) |
| reconcile-implementation-cost | [`declaration-name-plumbing`](../unicode-operators/clang/DEVIATIONS.md#declaration-name-plumbing) [`declaring-side-parse-cost`](../unicode-operators/clang/DEVIATIONS.md#declaring-side-parse-cost) [`flag-language-mode`](../unicode-operators/clang/DEVIATIONS.md#flag-language-mode) [`operator-candidate-assembly`](../unicode-operators/clang/DEVIATIONS.md#operator-candidate-assembly) [`expression-node-cost`](../unicode-operators/clang/DEVIATIONS.md#expression-node-cost) [`serialization-tooling-cost`](../unicode-operators/clang/DEVIATIONS.md#serialization-tooling-cost) [`ast-node-shape`](../unicode-operators/clang/DEVIATIONS.md#ast-node-shape) [`codegen-dispatch-sites`](../unicode-operators/clang/DEVIATIONS.md#codegen-dispatch-sites) |
| reconcile-declaring-using | [`ucd-version-drift`](../unicode-operators/clang/DEVIATIONS.md#ucd-version-drift) [`disjointness-evidence`](../unicode-operators/clang/DEVIATIONS.md#disjointness-evidence) [`over-oper-restrictions`](../unicode-operators/clang/DEVIATIONS.md#over-oper-restrictions) [`operator-id-anywhere`](../unicode-operators/clang/DEVIATIONS.md#operator-id-anywhere) [`infix-parse-cost`](../unicode-operators/clang/DEVIATIONS.md#infix-parse-cost) [`prefix-arity-selection`](../unicode-operators/clang/DEVIATIONS.md#prefix-arity-selection) [`operand-sequencing`](../unicode-operators/clang/DEVIATIONS.md#operand-sequencing) |
| reconcile-remainder | [`exclusion-list-derivation`](../unicode-operators/clang/DEVIATIONS.md#exclusion-list-derivation) [`ucn-operator-spellings`](../unicode-operators/clang/DEVIATIONS.md#ucn-operator-spellings) [`exclusion-diagnostics`](../unicode-operators/clang/DEVIATIONS.md#exclusion-diagnostics) [`clang-format-user-operators`](../unicode-operators/clang/DEVIATIONS.md#clang-format-user-operators) [`feature-coupling`](../unicode-operators/clang/DEVIATIONS.md#feature-coupling) [`replay-ordering`](../unicode-operators/clang/DEVIATIONS.md#replay-ordering); [`wrapper-inner-shape`](../DEVIATIONS.md#wrapper-inner-shape) [`analysis-layer-sites`](../DEVIATIONS.md#analysis-layer-sites) [`type-slot-cost`](../DEVIATIONS.md#type-slot-cost) [`cir-backtick-arms`](../DEVIATIONS.md#cir-backtick-arms); [`gcc-type-slot-parity`](../gcc/DEVIATIONS.md#gcc-type-slot-parity) | — **all done 2026-09-06, plus four this row did not list**: [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators) (clauses (4) and (5); the row's own gate could not otherwise pass), and the outstanding halves of [`infix-parse-cost`](../unicode-operators/clang/DEVIATIONS.md#infix-parse-cost), [`prefix-arity-selection`](../unicode-operators/clang/DEVIATIONS.md#prefix-arity-selection) and [`ast-node-shape`](../unicode-operators/clang/DEVIATIONS.md#ast-node-shape). [`gcc-type-slot-parity`](../gcc/DEVIATIONS.md#gcc-type-slot-parity) was taken by gcc-resync and not re-done.
| [settle-paper-rows](steps/settle-paper-rows.md) **(step inserted 2026-09-07)** | [`escape-name-positions`](../DEVIATIONS.md#escape-name-positions) [`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape) [`escape-alias-name-parity`](../gcc/DEVIATIONS.md#escape-alias-name-parity) [`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling) — the same four as the backlog table above, listed here too because they are deviation rows and this is the table a reader greps. **Two are `FIXED and RECONCILED` as of 2026-09-07** ([`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape), [`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling)); **two stay `OPEN` and are the author's**, and they are one question, not two. The claim that all three ledgers are clear — true for one day, 2026-09-06 — becomes true again when that question is answered and its answer is written into §12, and not before |

[`prefix-arity-selection`](../unicode-operators/clang/DEVIATIONS.md#prefix-arity-selection) and [`operand-sequencing`](../unicode-operators/clang/DEVIATIONS.md#operand-sequencing) are decided by decision-brief and *written* by reconcile-declaring-using — a decision
and its documentation are different steps, and implement-decisions owns only the ones that
turn into code. [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators) is split: its mangling clause is mangling-abi's, its postfix
substance is decision-brief's.

7 design decisions: four in decision-brief, the ABI in mangling-abi, U§6's example in reconcile-remainder, and
[`operand-sequencing`](../unicode-operators/clang/DEVIATIONS.md#operand-sequencing) recorded in reconcile-declaring-using as the CWG question it is.

**decision-brief's four were answered on 2026-09-06** — with a fifth, the
"anywhere" re-triage — and all five accepted the recommendation, which was
"change no code" in every case. See [`docs/open-decisions.md`](../../docs/open-decisions.md).
The consequence for this plan is that **implement-decisions has an empty scope
and is marked not-applicable**, and the work moves to the reconcile steps:
U§7 "Declaring" and U§7.1 to reconcile-declaring-using (with a new decision
entry owed, suggested slug `static-member-operators`), U§13's missing fold
bullet and U§13.1's treatment to reconcile-remainder, and one upstream report
to upstream-triage.

## Status log (each agent appends one row per branch or per document)
| Step | Date | Branch / doc | Commit | Gate result | Handoff |
|------|------|--------------|--------|-------------|---------|
| slug-the-ledgers | 2026-09-05 | `docs/backtick-operator-design.md` §3, `docs/unicode-operators.md` §2 | no branch — this repo only. 16 + 12 decisions converted from table rows to slug-headed sections with the full **Question / Status / Decision / Why / Log** shape plus `Decided by`; `Formerly:` keeps the old number. Two `Decision` cells were topics rather than decisions (`D2`, `D8`) and were restated. | **PASS** — every entry is an anchor, and every link into these two logs resolves (1029 local links checked, 1007 anchored, 0 broken). | [slug-the-ledgers](handoffs/slug-the-ledgers.handoff.md) |
| slug-the-ledgers | 2026-09-05 | `ops/DEVIATIONS.md`, `ops/gcc/DEVIATIONS.md`, `ops/unicode-operators/clang/DEVIATIONS.md` | 9 + 7 + 24 rows converted to slug-headed sections. Every entry now carries a `**Status:**` field; the Unicode ledger's 24 are all `OPEN`, which is accurate — it has never marked a row. | **PASS** — 40 entries, 40 anchors, 0 collisions. | same |
| slug-the-ledgers | 2026-09-05 | `ops/BACKLOG.md`, `ops/SLUGS.md` (new) | 38 defect rows converted to slug-headed sections keeping `Severity` / `Item` / `Where` / `Closed by`. `ops/SLUGS.md` maps all **106** retired identifiers both directions and records what was deliberately *not* renamed. | **PASS** — 106 mappings, both directions, every one resolving to a live anchor. | same |
| slug-the-ledgers | 2026-09-05 | cross-reference sweep: both papers, `ops/completion/**`, `ops/backlog/steps/BL05`–`BL07`, `CLAUDE.md`, `ops/AGENT_PROTOCOL.md`, `ops/HANDOFF_TEMPLATE.md` | 792 bare mentions rewritten as links to anchors; 19 internal identifiers **removed** from `papers/backtick-infix-and-keyword-escape.md` / `papers/unicode-mathematical-operators.md` rather than renamed, per the public-text rule. Protocol and ground rules now require slugs for anything added later. | **PASS** — the three gate greps are clean over `docs/`, `papers/`, `ops/*.md`, `ops/completion/` except `ops/SLUGS.md` (the map) and `steps/slug-the-ledgers.md` (the step's own quotation of what it retired); papers cite no identifier at all. | same |
| decision-brief | 2026-09-05 | `docs/open-decisions.md` (new), `ops/BACKLOG.md` §6 and its [dependent-template-operator-id](../BACKLOG.md#dependent-template-operator-id) row | no branch — this repo only. Five decision pages, each headed by its slug, each with the five required parts and an explicit *Branches touched* line; a summary table of question / recommendation / implementation consequence. Two slugs added — `fold-over-user-infix` (new; U§13's fold question had none) and `dependent-template-operator-id` (reused from the backlog row, per the prior handoff's reuse rule). §6 rewritten as links into the brief. | **PASS on all four gate bullets** — 5×5 required sections present, no `TBD` and no non-answer, every recommendation names its branches (all five are "none"; each rejected option names what it would touch), §6 restates nothing. Links: 61 in the new file, 0 broken; 1107 repo-wide, 0 broken. **PASS, then BLOCKED on the author** — no answer at that point, so no box ticked, no ledger `Status:` flipped and no decision `Log.` line appended. Unblocked 2026-09-06; see the next row. | [decision-brief](handoffs/decision-brief.handoff.md) |
| gcc-resync | 2026-09-06 | `backtick` (GCC), the re-sync | **not a plan step** — a rebase, treated as the R-prefixed Clang ones were. `c9ee2c5ab6c` (Daily bump, 2026-06-24) → `4df5e1e9b152` (Daily bump, 2026-09-06); 2158 upstream commits absorbed, 177 of them touching `gcc/cp`, `gcc/c-family` or `libcpp`. Ten feature commits replayed conflict-free; branch tip `b842ac1ed64` → `b50102ca0f6`. Pre-rebase tag `backtick-pre-resync` kept locally. | **PASS, and neutral**: `dg.exp=g++.dg/backtick/*.C` **88 passes / 0 failures** before the rebase and **88 / 0** after it, `EXIT=0` both times — the number G10 recorded. The feature diff's `+`/`−` lines are **byte-identical** across the move (`diff <(git diff c9ee2c5ab6c..pre) <(git diff 4df5e1e9b152..post)` differs only in blob hashes, hunk offsets and context). Closes [gcc-trunk-pin](../BACKLOG.md#gcc-trunk-pin). | [gcc-resync](handoffs/gcc-resync.handoff.md) |
| gcc-resync | 2026-09-06 | `backtick` (GCC), `edce709e170` | ADL for a template-id operator slot: `cp_parser_backtick_template_id_slot` in `gcc/cp/parser.cc`, used at both handler sites, plus `g++.dg/backtick/infix-adl-template-id.C`. Closes [template-id-slot-adl](../BACKLOG.md#template-id-slot-adl); new ledger row [gcc-template-id-slot-adl](../gcc/DEVIATIONS.md#gcc-template-id-slot-adl). | **PASS**: 88 → **94 passes / 0 failures**, the 6 being the new test (1 compile + 5 scan-tree-dump). Verified failing first — on the pre-fix binary `` ax `g<int>` ay `` gave *"'g' was not declared in this scope"*, *"expected primary-expression before 'int'"* and *"expected '`' before 'int'"*; after, the dump reads `r_pure = ns::g<int> (…)` and `r_augment = ns2::h<int> (…)`. | same |
| gcc-resync | 2026-09-06 | `backtick` (GCC), `daf5fa6feb0` | Module streaming of keyword-escaped names, **verified not broken**: `g++.dg/modules/backtick-escape-1_a.C` / `_b.C`. No production change. Closes [module-streaming-escapes](../BACKLOG.md#module-streaming-escapes). | **PASS**: `modules.exp=backtick-escape-1*` → **15 passes / 0 failures** (5 checks × 3 std variants), including the CMI being produced and both mangled names appearing in the importer's assembly. | same |
| gcc-resync | 2026-09-06 | `backtick` (GCC), `12d3b5b0c07` | Three over-permissive `flag_backtick` guards narrowed: the escape is now carried on the `cp_declarator` and required by `grokdeclarator`, and both `case CPP_BACKTICK:` arms re-test the token because other cases fall through to them. 5 files. Closes [grokdeclarator-guard-scope](../BACKLOG.md#grokdeclarator-guard-scope); new ledger row [escape-arm-entry-token](../gcc/DEVIATIONS.md#escape-arm-entry-token). | **PASS**: 94 → **103 passes / 0 failures**, `EXIT=0`; the 9 are three new `dg-error` checks × 3 variants in `escape-diag.C`. `void new (int, int);` and a stray `^` now diagnose **character-identically with and without `-fbacktick`**, which they did not before. Wider sweep, all **0 unexpected failures**: `g++.dg/parse` 4905, `lookup` 3437, `template` 9874, `overload` 692, `init` 3632, `expr` 952. | same |
| gcc-resync | 2026-09-06 | `docs/backtick-operator-design.md` §17.3 / §17.4, `ops/gcc/DEVIATIONS.md`, `ops/BACKLOG.md`, `ops/gcc/PLAN.md`, `CLAUDE.md` | §17.4 rewritten (ADL binds wherever the slot is an unqualified name, with or without template arguments; both compilers now deliver it and agree). §17.3 gains a status paragraph naming the one place the two implementations accept different programs. Ledger: [gcc-type-slot-parity](../gcc/DEVIATIONS.md#gcc-type-slot-parity) **RECONCILED**, [gcc-slot-adl](../gcc/DEVIATIONS.md#gcc-slot-adl) corrected to **RESOLVED** (its prose already said G10 fixed it), and three new entries — [gcc-template-id-slot-adl](../gcc/DEVIATIONS.md#gcc-template-id-slot-adl), [escape-arm-entry-token](../gcc/DEVIATIONS.md#escape-arm-entry-token), [gcc-wrapper-parity](../gcc/DEVIATIONS.md#gcc-wrapper-parity). Five `Closed by` cells filled. | **PASS on the docs gate**: every row this step claims is marked in its ledger and names the section *and* paragraph it landed in; the GCC ledger has **no `Status: OPEN` row left**. | same |
| decision-brief | 2026-09-06 | `docs/open-decisions.md`; `docs/unicode-operators.md` §2 and `docs/backtick-operator-design.md` §3; `ops/unicode-operators/clang/DEVIATIONS.md`; `ops/BACKLOG.md`; `ops/unicode-operators/clang/REPLAY.md` | no branch — this repo only. **Answered by the design author: all five recommendations accepted as written, none overridden.** Recorded as five dated subsections in the brief plus a where-it-went table; `Log.` entries appended to 5 decision entries ([unary-forms](../../docs/unicode-operators.md#unary-forms) carries three of the five answers, plus [operator-function-id](../../docs/unicode-operators.md#operator-function-id), [user-infix-precedence](../../docs/unicode-operators.md#user-infix-precedence), [operator-identifier-disjointness](../../docs/unicode-operators.md#operator-identifier-disjointness), [precedence-level](../../docs/backtick-operator-design.md#precedence-level)); 5 ledger rows marked **OPEN — DECIDED** with the step that owes the writing. Nothing turned into code, so implement-decisions is marked **not-applicable**; the Coverage table splits [dependent-template-operator-id](../BACKLOG.md#dependent-template-operator-id) into its reword half and its report half; the silent fold guard is recorded in the gate facts and at the top of `REPLAY.md`. | **PASS** — box ticked. 5 answers recorded, 5 `Log.` entries appended, 5 ledger `Status:` fields marked, 1 backlog `Closed by` corrected. No `RECONCILED` marker set anywhere: **deciding is not reconciling**, and every row names its destination section and its owner. | [decision-brief](handoffs/decision-brief.handoff.md) |
| upstream-reports | 2026-09-06 | [`increment-decrement-mangling`](../BACKLOG.md#increment-decrement-mangling) → [draft](upstream-drafts/increment-decrement-mangling.md) | no branch — this repo only. Re-confirmed on two trunk builds (`a815e6f267c1`, `d28193fa1ff6`) for `++` **and** `--`, against `g++ 15.2.0`; both mangler sites (`mangleOperatorName`'s `OO_PlusPlus`/`OO_MinusMinus` arms, `mangleExpression`'s `UnaryOperatorClass` and `CXXOperatorCallExprClass` arms) read out of trunk `72417eb739e5` itself. ABI citation found: **§5.1.3** *Operator Encodings* and **§5.1.6** *Expressions*. `llvm-cxxfilt` round-trips all four spellings. **The reproducer on file does not reproduce** — it needs explicit instantiations. | **PASS on the amended gate** — confirmed on current trunk, 8 duplicate searches (no duplicate; #26427 and #25168 checked and rejected), report written out in full. The step's original "three issue URLs" bullet is **not met, by the author's decision** not to file. | [upstream-reports](handoffs/upstream-reports.handoff.md) |
| upstream-reports | 2026-09-06 | [`clangir-lvalue-crash`](../BACKLOG.md#clangir-lvalue-crash) → [draft](upstream-drafts/clangir-lvalue-crash.md) | no branch — this repo only. **Scope widened by the confirmation:** the `default:` arm is one of **21** arms in `emitLValue` that `errorNYI(...)` then `return LValue()`, and the enumerated ones are reachable from stock C++26. Reproduced with `PackIndexingExpr` in l-value position, no feature and no flag: `p...[0] = 1;` → BL04's exact `QualType::getCommonPtr` assertion; `return p...[0];` → segfault in `createStore` from `emitReturnStmt`. | **PASS on the amended gate** — the scratch CIR build (`6ee1358f7b47`, base `bb33de72920a`) differs from trunk `72417eb739e5` in `emitLValue` **only** by BL04's two feature arms, and `emitReturnStmt` is byte-identical, so the exercised path is today's trunk code. 7 duplicate searches; no duplicate, #202097 and #214443 recorded as same-shape prior art. No URL, by decision. | same |
| upstream-reports | 2026-09-06 | [`unqualified-id-union-read`](../BACKLOG.md#unqualified-id-union-read) → [draft](upstream-drafts/unqualified-id-union-read.md) | no branch — this repo only. **The cited line moved and the defect is not where the row said.** `ParseExprCXX.cpp:2297`'s block is correct on trunk (it kind-tests before reading `OperatorFunctionId`). The live read is the `OpKind` ternary at `ParseExprCXX.cpp:2374` (`ParseUnqualifiedIdTemplateId`), which excludes only `IK_Identifier` — plus a **second, unrecorded** copy at `ParseTemplate.cpp:1155` (`AnnotateTemplateIdToken`), unguarded. `Identifier` is the active member for `IK_LiteralOperatorId` (`DeclSpec.h`), and `OFI::Operator` is `OFI`'s first member. | **PASS on the amended gate** — confirmed by reading trunk `72417eb739e5`; still latent (`operator""_x<'1','2'>()` compiles clean on both builds). 4 duplicate searches: no duplicate, and **#20143** found — the same read at `Declarator::isStaticMember()`, now kind-guarded, which settles the fix shape. No URL, by decision. | same |
| upstream-reports | 2026-09-06 | `docs/unicode-operators.md` §9 and §13.1; `papers/unicode-mathematical-operators.md` Acknowledgments; [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators) clause (c); `ops/BACKLOG.md` ×3 | The three places that carry the mangling defect as a private note now cite it — **with the placeholder token `LLVM-ISSUE-PENDING` where the issue number goes**, since there is none. §13.1's code block gains the missing explicit instantiations and the `--` twin; §9's closing gains the fixity-marker paragraph that [mangling-abi](steps/mangling-abi.md) needs; the paper gains `--`, the two ABI section numbers, and the placeholder. Clause (3) of [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators)'s recommended doc changes was **not** touched: that is mangling-abi's. | **PASS on the docs gate** — every row this step touched is marked in its ledger and names the section *and* paragraph. `grep -rn LLVM-ISSUE-PENDING docs papers ops` finds every mention in one pass, four of them substitution points; no row is marked closed that is not. | same |
| upstream-triage | 2026-09-06 | [`inner-call-source-range`](../BACKLOG.md#inner-call-source-range), [`operator-caret-range`](../BACKLOG.md#operator-caret-range) → **WONTFIX, closed** | no branch — this repo only. Both reasons verified rather than accepted. The caret range was reproduced in **stock C++23 with no feature flag** on `a815e6f267c1` and `783a9c1a5f6f`: `operator+(a, a)` and `operator""_x(a)` each underline exactly 8 columns, character-identically to the glyph case. The source-range argument was checked against upstream's own precedent: for `p < q`, `CXXRewrittenBinaryOperator` is `<col:28, col:32>` and the `CXXOperatorCallExpr` it wraps is `<col:28, col:30>`. Design docs: new `docs/backtick-operator-design.md` **§17.5** plus a dated `Log.` entry on [source-fidelity-node](../../docs/backtick-operator-design.md#source-fidelity-node); `docs/unicode-operators.md` §7 "Desugaring" (last sentence) and a new §10 subsection [operator-name-caret-range](../../docs/unicode-operators.md#operator-name-caret-range). | **PASS** — both rows have a `Closed by` giving a reason a reader who has not read this plan would accept, and both reasons are in a design doc as well as the row. **Row corrected:** [`inner-call-source-range`](../BACKLOG.md#inner-call-source-range)'s re-grade was wrong about the cost — `CallExpr::setUsesMemberSyntax()` is public and recomputes the cached begin loc from arg 0, so no `CallExpr::Create` overload is needed; it is declined on meaning, not cost. | [upstream-triage](handoffs/upstream-triage.handoff.md) |
| upstream-triage | 2026-09-06 | [`cxxfilt-stdin-nonascii`](../BACKLOG.md#cxxfilt-stdin-nonascii), [`auto-return-round-trip`](../BACKLOG.md#auto-return-round-trip), [`pch-ast-print-order`](../BACKLOG.md#pch-ast-print-order) → **REPORT, drafted, rows stay open** | no branch — this repo only. Three drafts added to [`upstream-drafts/`](upstream-drafts/README.md) in the shape upstream-reports established; its README now tables all six and carries the seventh-report explanation. All three reproduced on `783a9c1a5f6f` (feature flags off) and confirmed to be trunk code: `llvm-cxxfilt.cpp` and `DeclBase.cpp` are **byte-identical** between that build's base `d28193fa1ff6` and trunk `72417eb739e5`, and `DeclPrinter::VisitFunctionDecl` extracted from both is identical. | **PASS on the amended gate** — 18 duplicate queries, no duplicate for any of the three. Findings carried into the drafts: **#24794** (open) is the *same two functions* as the PCH defect, its "expels decls" half fixed and the ordering half surviving, so that report is probably a comment on it; **#39337** is the request that introduced the cxxfilt splitter; **#12178 / #218420 / #147150** are the `-ast-print` family. **Two row corrections:** [`pch-ast-print-order`](../BACKLOG.md#pch-ast-print-order)'s obvious reproducer does **not** reproduce — the class must be *used* from the main file, not merely present in the PCH; and **#178767 is not [`cxxfilt-stdin-nonascii`](../BACKLOG.md#cxxfilt-stdin-nonascii)**, contrary to the prior handoff's forward note — that symbol fails on the argv path too. | same |
| upstream-triage | 2026-09-06 | [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id) report half → **NOT DONE, returned to the author**; `docs/open-decisions.md`, [operator-id-anywhere](../unicode-operators/clang/DEVIATIONS.md#operator-id-anywhere) | no branch — this repo only. **No draft written.** The literal-operator reproducer `t.template operator""_lit<int>(0)` is *correctly* rejected: [over.literal]/1 forbids a member literal operator, Clang rejects the member, static-member and member-template spellings and GCC agrees, and trunk `SemaTemplate.cpp` handles `IK_LiteralOperatorId` deliberately with the comment *"can never occur in a dependent scope (literal operators can only be declared at namespace scope)."* Filing it would report correct behaviour as a bug. Recorded as a dated entry in the brief's Answers, the summary-table row annotated, and the ledger row's false clause struck. | **Gate not applicable — this item is not one of the step's five, and it ends by asking rather than choosing.** The premise the 2026-09-06 answer rested on is falsified: literal operators share the *code path* but suffer no limitation from it, so the limitation is **exclusive to the new name kind**, and the clause "a limitation user-defined literal operators have had since C++11" must not enter U§7.1. The reword half is unblocked and unaffected. | same |
| mangling-abi | 2026-09-06 | `docs/unicode-operators.md` §9 (rewritten) and [operator-mangling](../../docs/unicode-operators.md#operator-mangling) | no branch — this repo only; nothing built, no feature branch touched. §9 is now three slug-headed subsections instead of five paragraphs: [mangling-derivation-rule](../../docs/unicode-operators.md#mangling-derivation-rule) (the derivation **rule** rather than the example, the injectivity argument, the unexercised padding/astral branches, the two-demangler measurement with four symbol forms, the retained math-identifier disjointness paragraph, and the inherited arity-digit wrinkle), [abi-production-request](../../docs/unicode-operators.md#abi-production-request) (the open decision, in decision-brief's five-part shape), and [microsoft-abi-position](../../docs/unicode-operators.md#microsoft-abi-position). One new ABI fact, read out of the ABI HTML rather than a handoff: **§5.1.3's prose scopes `v <digit> <source-name>` to "vendors who define builtin extended operators (e.g. `__imag`)"**, which a user-declared operator is not — the strongest argument in the section for asking for a first-class production. [operator-mangling](../../docs/unicode-operators.md#operator-mangling)'s Status/Decision/Why/Log updated to point at the three subsections. | **PASS on the docs gate, then BLOCKED on the author.** §9 answers all three of what is implemented / what is asked for / what is unexamined, and cites `LLVM-ISSUE-PENDING` (no issue number exists; upstream-reports drafted and did not file). The *asked for* answer is a **recommendation, not a choice** — three options with costs, per the plan's Decide ground rule — so the box stays unticked. 1191 local links checked repo-wide, **0 broken**. | [mangling-abi](handoffs/mangling-abi.handoff.md) |
| mangling-abi | 2026-09-06 | `ops/unicode-operators/clang/DEVIATIONS.md` ×3, `ops/BACKLOG.md`, `CLAUDE.md` | [`vendor-extended-mangling`](../unicode-operators/clang/DEVIATIONS.md#vendor-extended-mangling) → **RECONCILED**, naming the block quote, the *"Two branches … unexercised by construction"* paragraph, the *"'Demangler-tolerated' undersells the measurement"* paragraph and the closing arity-digit paragraph. [`msvc-mangling`](../unicode-operators/clang/DEVIATIONS.md#msvc-mangling) → **RECONCILED**, naming all three paragraphs of [microsoft-abi-position](../../docs/unicode-operators.md#microsoft-abi-position). [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators) — clause (3) **taken and closed** into [abi-production-request](../../docs/unicode-operators.md#abi-production-request)'s third measured bullet and Recommendation point 1; clauses (1), (4), (5) stay open and stay reconcile-remainder's. [`astral-plane-mangling`](../BACKLOG.md#astral-plane-mangling) `Closed by` filled — recorded, not fixed, because it is a property of the frozen [token-set](../../docs/unicode-operators.md#token-set). Ledger header corrected: it no longer claims no row has ever been reconciled. `CLAUDE.md` Layout gains `docs/open-decisions.md` and an ABI clause for §9 — the plan's standing Housekeeping item, now done. | **PASS** — every row this step claims names the destination **section and paragraph**, and no row is marked closed that is not: the three upstream-report rows and the 22 other Unicode deviation rows are untouched and still `OPEN`. | same |
| mangling-abi | 2026-09-06 | `docs/unicode-operators.md` [abi-production-request](../../docs/unicode-operators.md#abi-production-request) and [operator-mangling](../../docs/unicode-operators.md#operator-mangling); `docs/open-decisions.md` | **Answered by the design author: the recommendation accepted as written** — *(a) and (b) together, non-normatively.* Recorded where the question lives, U§9, because for this question the answer **is** the section: `Status: open` becomes a dated answer paragraph saying the paper describes the vendor form as the fallback needing no ABI action, then asks for a first-class production **as a request rather than as proposed wording**, `s` reserved so postfix stays takeable and the letters left to the ABI group. [operator-mangling](../../docs/unicode-operators.md#operator-mangling)'s Status, Decided by and Log updated — the entry stays `Proposed` on the same terms as the rest of the log, i.e. it is polled with the paper, and no longer because anything in it is undecided. `docs/open-decisions.md` gains the dated answer, a where-it-was-recorded row, and a front-matter correction: the three questions whose *pages* live elsewhere still have their *answers* recorded there. | **PASS — box ticked.** All four gate bullets met and the answer is recorded in four places (U§9, the decision entry's Log, the brief's Answers, the where-recorded table). [`vendor-extended-mangling`](../unicode-operators/clang/DEVIATIONS.md#vendor-extended-mangling) gains the note that its closing argument is now the paper's position; [`astral-plane-mangling`](../BACKLOG.md#astral-plane-mangling)'s conditional resolves — the untested-branches paragraph **does** reach the paper, and [unicode-paper](steps/unicode-paper.md) carries it. | [mangling-abi](handoffs/mangling-abi.handoff.md) |
| mangling-abi | 2026-09-06 | `docs/open-decisions.md`; [`operator-id-anywhere`](../unicode-operators/clang/DEVIATIONS.md#operator-id-anywhere); [`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id) | **Second answer, recorded only — none of the work it generates was done here.** [dependent-template-operator-id](../../docs/open-decisions.md#dependent-template-operator-id)'s reopened report half is settled as **option (a): reword only, no upstream report.** The gap is this feature's own — `DependentTemplateStorage` predates the new name kind, the same thesis as [`declaration-name-plumbing`](../unicode-operators/clang/DEVIATIONS.md#declaration-name-plumbing) and [`operator-candidate-assembly`](../unicode-operators/clang/DEVIATIONS.md#operator-candidate-assembly) — and the struck clause *"a limitation user-defined literal operators have had since C++11"* is false and must not enter U§7.1 or either paper. The summary-table row no longer reads as partly reopened; the ledger row drops its report obligation and points at the corrected wording; the backlog row's `Closed by` says it closes on the reword alone, owned by [reconcile-declaring-using](steps/reconcile-declaring-using.md), and its **Item** — which still asserted the false claim — carries a dated correction. | **PASS on the recording gate.** Nothing upstream is pending for this row and no draft is owed; [upstream-triage](steps/upstream-triage.md) is done with it. **The reword itself was not written** — it is [reconcile-declaring-using](steps/reconcile-declaring-using.md)'s, and the corrected justifying clause is now in three places it will look. | same |
| clang-paper-truth | 2026-09-06 | `backtick-trunk`, `5f70443430b8` | Three defects in one commit, all gated on `-fbacktick`. (1) `defm backtick` gains `ShouldParseIf<cplusplus.KeyPath>` + `clang/test/Lexer/backtick-c-mode.c`, carried back from the Unicode branch. (2) `BacktickInfixExpr::getOperand(unsigned)` (new, in `Expr.cpp` beside `getCallExpr()`) recovers the operands as written from the three shapes the pretty-printer already reconstructs from — call, `CXXTemporaryObjectExpr`, `CXXUnresolvedConstructExpr` — and `getBeginLoc`/`getEndLoc`/`getSourceRange` are computed from it. (3) `PrintingPolicy::BacktickKeywordEscape`, set from `LangOptions::Backtick`, plus six routing sites: `DeclarationName::print`, three `DeclPrinter` declarator printers via the new `writtenDeclName`, `StmtPrinter::VisitMemberExpr`, `ASTDiagnostic`'s `ak_declarationname` — and `TextNodeDumper::VisitMemberExpr` gated the *other* way so `-ast-dump` stays bare. | **PASS**: **54109 / 48223 passed / 0 failed** / 27 XFAIL / 5853 unsupported / 6 skipped, `EXIT=0`, unfiltered. Baseline 54108/48222/0, so `+1` discovered and `+1` passed — the one new test file. **All three fixes demonstrated failing first** on the pre-fix binary, using the new tests' own content: C-mode exits **0** with the flag (the test's `not` and its `diff` both fail); ranges read `<col:16, col:19>` against the pinned `<col:13, col:21>`; `-ast-print` emits `void new();` and re-parsing it gives 13 errors. **One environment reading, not budgeted**: an earlier completed run showed `DirectoryWatcherTest/InitialScanAsync` failing under load 27; confirmed environmental against the pristine `build-main` binary (8/8 pass), the new feature binary (8/8, and `--gtest_repeat=5` on that case), and `fs.inotify.max_user_watches` = 524288. The re-run on the settled build is the row's numbers. | [clang-paper-truth](handoffs/clang-paper-truth.handoff.md) |
| clang-paper-truth | 2026-09-06 | `backtick-23`, `2f4f59444d07` | Cherry-pick of `5f70443430b8`, **one conflict**: `PrettyPrinter.h`'s constructor initialiser list, because the 23.x base lacks `PrettyEnums`. Resolved by keeping the branch's list and appending `BacktickKeywordEscape(LO.Backtick)` last — the field must stay declared last too, after `SuppressLambdaBody`, or the initialiser-order warning fires. Everything else applied clean. | **PASS**: **54343 / 48501 passed / 1 failed** / 27 XFAIL / 5808 unsupported / 6 skipped. The 1 is [`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config) (`Clang :: Format/dump-config-objc-stdin.m`), expected on this branch and nothing else. Baseline 54342/48500/1: `+1` discovered, `+1` passed. Gated **independently**, not inferred from the clean pick. | same |
| clang-paper-truth | 2026-09-06 | `docs/backtick-operator-design.md` §3 / §6.4 / §6.5 / §12 / §17.5; `ops/DEVIATIONS.md`; `ops/unicode-operators/clang/DEVIATIONS.md`; `ops/BACKLOG.md` ×3 | New decision entry [keyword-escape-printing](../../docs/backtick-operator-design.md#keyword-escape-printing) — the escape is part of the name's *spelling*, so `-ast-print` **and diagnostics** re-escape it while `-ast-dump` stays bare, that being the evidence the name is an ordinary identifier. Dated `Log.` entries on [feature-gating](../../docs/backtick-operator-design.md#feature-gating) and [source-fidelity-node](../../docs/backtick-operator-design.md#source-fidelity-node). §6.4's *"enough for source ranges"* clause corrected; §6.5 gains the `ShouldParseIf` sentence; §12 gains **Printing and diagnostics**; §17.5 gains the paragraph saying how the range is computed and that until today the section described an intention. Ledger: [backtick-source-locations](../DEVIATIONS.md#backtick-source-locations) gains a dated **Correction** (it stays `RECONCILED`; the corrected clause is *"sufficient … for source ranges"*), new row [keyword-escape-printing](../DEVIATIONS.md#keyword-escape-printing) → **RECONCILED** on the day it was written, and [flag-language-mode](../unicode-operators/clang/DEVIATIONS.md#flag-language-mode) gains a dated **Note** — its `Status:` untouched, it stays [reconcile-implementation-cost](steps/reconcile-implementation-cost.md)'s. | **PASS on the docs gate** — every row this step claims to close names its destination **section and paragraph**, and no row is marked closed that is not: the ledger's other 8 rows and the Unicode ledger's 22 are untouched. 1612 local links checked repo-wide, 8 broken, **all 8 pre-existing and none touched here**. **One gate fact corrected**: `Parser::isFoldOperator`'s guard is `prec::Backtick` on the backtick branches and `prec::UserInfix` only on the Unicode ones, so the grep this plan prescribed came back empty on a backtick branch and looked exactly like the silent loss it warns about. All four verified intact. | same |
| M2 | 2026-09-06 | `unicode-operators-experiment` | `85734d71ce1c` — **merge commit**, `git merge --no-ff 5f70443430b8` (the explicit commit, per M1's rule that `backtick-trunk` moves), bringing `5a2b586463d9` (BL02, D16 type slot), `cfbc69be9d4f` (BL04's backtick CIR arms) and `5f70443430b8` ([clang-paper-truth](steps/clang-paper-truth.md)). **Eight conflicts, none semantic.** Already-applied, resolved to this branch's version: `Options.td` (the `ShouldParseIf<cplusplus.KeyPath>` guard on `defm backtick` came *from* here at `U04`) and `test/Lexer/backtick-c-mode.c` (bodies byte-identical, only the header comment differs). Both sides added at the same anchor, resolved by keeping both: `Sema.h` and `SemaExpr.cpp`, where BL02's `ActOnBacktickOperator(ParsedType…)` overload now sits beside its expression sibling and `ActOnUserOperator` after them. The four CIR files, resolved to this branch's version: the arms are textually identical and only the shared lead comments conflicted, this branch's already naming both wrappers. `getOperand(unsigned)` came across whole and was **not** narrowed to `getCallExpr()` — BL02's type slot arrives in the same merge, so the five type-slot nodes exist here from the commit that fixes them. | **`check-clang` GREEN, unfiltered, `EXIT=0`** — **54183 discovered / 48295 passed / 0 failed** / 27 XFAIL / 5855 unsupported / 6 skipped, 370.79 s. **Exactly the Baselines row, with no arithmetic to explain**: the two test files the merge would have added, `Lexer/backtick-c-mode.c` and `CIR/CodeGen/backtick-infix.cpp`, already existed here. Build `EXIT=0`, 3000 targets (four widely-included headers are in the merge), **zero `warning:` lines**. The first run reported 3 `DirectoryWatcherTest.*` failures under load 23–27; confirmed environmental and **not** budgeted — `max_user_watches` 524288, all 8 pass on the pristine `build-main` binary and 5× on this one — and the re-run on the settled build is the row above. **The fold guard was proven, not read**: deleting `&& Level != prec::UserInfix` makes `Parser/unicode-operator-precedence.cpp` fail at line 322, and only the **right** fold pins it. | [M2-forward-port](handoffs/M2-forward-port.handoff.md) |
| M2 | 2026-09-06 | `unicode-operators-experiment` | `de76585ae45d` — the one-line-comment defect nobody owned: `CheckUserOperatorDeclaration`'s `isStatic()` guard carried the *"a static member has no implicit object parameter"* reason that [over-oper-restrictions](../../docs/open-decisions.md#over-oper-restrictions) explicitly rejected on 2026-09-06, the arity rule counting operands and a two-parameter static member satisfying it. Replaced with the ruled reason — the desugaring equivalence is defined over exactly two spellings and a static member names neither. Comment only, +6/−4, guard untouched. **`unicode-operators-upstream` carries the identical comment and was NOT touched**: M2 names the experiment branch only. The two Unicode branches now differ by that paragraph; the author's call. | Covered by the gate above. | [M2-forward-port](handoffs/M2-forward-port.handoff.md) |
| null-return-suppression | 2026-09-06 | `unicode-operators-experiment`, `1f0790897357` | Fixed here first, as the step file directs, because this branch carries **both** wrappers and one TU can hold all four spellings. One arm per wrapper in `peelOffOuterExpr` (`clang/lib/StaticAnalyzer/Core/BugReporterVisitors.cpp`), beside the `FullExpr` and `OpaqueValueExpr` arms already there. **Not** at the `CallEvent::isCallStmt` test the row names — that was tried first, rebuilt and measured, and changes nothing, because `Tracker::track` gives up three frames earlier: `findNodeForExpression` finds no exploded-graph node for a wrapper, since the CFG has no element for one. Both Analysis tests restructured: the `-verify` half now runs **twice**, `-verify=expected,nosupp` with `suppress-null-return-paths=false` and `-verify=expected` at the default, so a line with a `nosupp-` directive and no `expected-` one asserts *both* that the report is emitted with the suppression off and that it is not with the suppression on. New operand-dereference cases keep a bug-finding assertion live at the default setting. | **PASS**: **54183 / 48295 passed / 0 failed** / 27 XFAIL / 5855 unsupported / 6 skipped, `EXIT=0`, unfiltered — **exactly the Baselines row**, and expected to be: no test file was added, only amended. Parity measured before and after in one TU: at the default setting 3 operator forms reported and the explicit call did not; now none reports, and with the suppression off all four do, with byte-identical 8-note paths (it was 2 notes vs 8 before). **`bugs_are_still_found` verified still a test**: with `LiveVariables::LookThroughExpr`'s arm reverted and clang rebuilt, its assertion fails; the arm was restored and the tree verified byte-identical before committing. `git-clang-format` clean; zero `warning:` lines. | [null-return-suppression](handoffs/null-return-suppression.handoff.md) |
| null-return-suppression | 2026-09-06 | `unicode-operators-upstream`, `1622a37196c0` | The `UserOperatorExpr` half alone, produced in the experiment tree and diffed line by line against this worktree before committing (BL04's method, as the step file asks). The `if` lines are byte-identical to the experiment branch's; the lead comment is a single-feature rewrite, anchored against the *upstream* precedents rather than beside a backtick arm. Test file byte-identical to the experiment branch's. `git show HEAD \| grep -i backtick` → **0**. | **PASS**: **54241 / 48323 passed / 0 failed** / 27 XFAIL / 5885 unsupported / 6 skipped, `EXIT=0`, unfiltered — exactly the Baselines row. Gated **independently**. This build dir needed a 1229-target rebuild on first touch (the stale host-compiler PCH [clang-paper-truth](handoffs/clang-paper-truth.handoff.md) predicted for both Unicode dirs) plus ~1049 more for `AllClangUnitTests`. | same |
| null-return-suppression | 2026-09-06 | `backtick-trunk`, `c1c6af4dd620` | The `BacktickInfixExpr` half alone, same method. **The build died first with `error: PCH file … uses an older format that is no longer supported`** — the host `llvm-23` package was upgraded on 2026-09-05, and both backtick build dirs still held PCHs written by the old one. Not a diff problem: `find <build> -name cmake_pch.hxx.pch -delete` and rebuild (2981 targets). This is the same host-upgrade trap [clang-paper-truth](handoffs/clang-paper-truth.handoff.md) hit mid-run, in its other form — there it invalidated the PCH silently and rebuilt everything, here ninja thought the PCH current and the compile failed. | **PASS**: **54109 / 48223 passed / 0 failed** / 27 XFAIL / 5853 unsupported / 6 skipped, `EXIT=0`, unfiltered — exactly the Baselines row as [clang-paper-truth](steps/clang-paper-truth.md) updated it. `git-clang-format` clean; zero `warning:` lines; the `prec::Backtick` fold guard read out of the worktree and intact. | same |
| null-return-suppression | 2026-09-06 | `backtick-23`, `ae8a7cf727c3` | Cherry-pick of `c1c6af4dd620`, **no conflict** — `peelOffOuterExpr`'s neighbourhood is byte-identical on the two bases — and the picked hunks verified identical to trunk's apart from blob hashes. Same stale-PCH rebuild as trunk (2968 targets). | **PASS**: **54343 / 48501 passed / 1 failed** / 27 XFAIL / 5808 unsupported / 6 skipped — exactly the Baselines row; the 1 is [`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config), expected on this branch and nothing else. Gated **independently**. **The first run reported 4 failures and three were the environment**, confirmed and **not** budgeted: `DirectoryWatcherTest.DeleteWatchedDir` / `ModifyFile` / `InitialScanAsync`, all with `No space left on device : inotify_add_watch()`. `fs.inotify.max_user_watches` is 524288 and **524092 were in use, 523717 of them held by `cloud-drive-dae`** — [`inotify-watch-budget`](../BACKLOG.md#inotify-watch-budget) exactly as this plan's gate facts describe it, the hoard having grown with four freshly rebuilt trees. All 8 pass on the pristine `~/src/llvm/build-main` binary and 3× on this branch's own binary; the re-run on the settled machine is the row's numbers. **This is the third consecutive Clang step to hit a `DirectoryWatcherTest` failure**, and the first where the cause is watch exhaustion rather than a timing flake — the error text tells the two apart. | same |
| null-return-suppression | 2026-09-06 | `docs/backtick-operator-design.md` §17 / §17.6; `docs/unicode-operators.md` §8; `ops/DEVIATIONS.md`; `ops/unicode-operators/clang/DEVIATIONS.md`; `ops/BACKLOG.md`; `ops/unicode-operators/clang/REPLAY.md` | New **§17.6, "The wrapper and the static analyzer"** — the obligation stated for a reviewer: a source-fidelity wrapper owes the analyzer *parity with the desugared call*, it is **seven** sites and not six, the seventh is created by meeting the other six, nothing forces it, and a parity test can be built on the very defect it should catch. §17's intro no longer says "four points". Third dated `Log.` entry on [source-fidelity-node](../../docs/backtick-operator-design.md#source-fidelity-node). U§8 gains a **"The static analyzer, which no site list contains"** bullet in the Clang list, pointing at §17.6 rather than duplicating it — deliberately, because the finding is that *both* features had the identical defect in the identical place, so it belongs to wrapper nodes and not to either feature. Ledgers: dated **Notes** on [analysis-layer-sites](../DEVIATIONS.md#analysis-layer-sites) (its count is seven, not the five its recommendation says) and on [expression-node-cost](../unicode-operators/clang/DEVIATIONS.md#expression-node-cost) (this obligation is off its link/unreachable/warning/silence axis entirely — it is created by meeting another obligation); both keep their `Status:` and both stay their own steps'. `REPLAY.md` gains a section classifying the Unicode arm `upstream replay` and warning that a replay landing BL03's six and stopping reproduces the defect. | **PASS on the docs gate** — the one row closed names its destination **section and paragraph**, and its wrong mechanism is corrected in place rather than quietly overwritten, because a reader following it would repeat the half-hour spent on the `isCallStmt` test. 1740 local links checked repo-wide; excluding the vendored `papers/wg21/` tree, **4 broken, all four pre-existing** (`<path>`/`…` placeholders in handoff prose). **Caveat for the next link check:** GitHub does *not* collapse whitespace when slugifying, so `### 2026-09-06 — foo` is `#2026-09-06--foo` with two hyphens and those links are correct — a collapsing checker reports them broken, which is where [clang-paper-truth](handoffs/clang-paper-truth.handoff.md)'s "8 broken, all pre-existing" came from. | same |
| evidence-debt | 2026-09-06 | `backtick-trunk`, `2b7471f6bc13` | `clang/test/AST/backtick-template-print.cpp` (+90), the only change on this branch — no production edit. Five shapes, each printed as pattern *and* as instantiation. | **PASS, `EXIT=0`, 0 failures**: 54110 discovered / 48224 passed / **0** / 27 XFAIL / 5853 unsupported / 6 skipped, 496 s. Exactly the [clang-paper-truth](handoffs/clang-paper-truth.handoff.md) baseline **+1 discovered / +1 passed** — a lit test, so it lands in *passed*, not in *unsupported*. **The test was proven load-bearing before it was committed**: with `TreeTransform::TransformBacktickInfixExpr` returning `Inner` instead of rebuilding the wrapper, the new file **fails** and the pre-existing `clang/test/Parser/backtick-ast-print.cpp` **passes** — the patterns keep printing the backtick form while every instantiation reverts to `add(a, b)`, `mk(a, b)`, `f(a, b)`, `Pair2(a, b)`. The tree was restored byte-identical and rebuilt before the gate. **The first gate run reported 6 failures and all six were the machine** — `DirectoryWatcherTest.*` with `No space left on device : inotify_add_watch()`, and all 8 failing on the pristine `build-main` binary at that moment. Cause identified rather than waited out: this step's own `~/src/llvm/build-lldb-scratch-upstream` had just added 2247 directories. Removing it made all 8 pass on `build-main` immediately, and the re-run is the row above. | [evidence-debt](handoffs/evidence-debt.handoff.md) |
| evidence-debt | 2026-09-06 | `backtick-23`, `99d75995e800` | Cherry-pick of `2b7471f6bc13`; patch text identical, file byte-identical, no conflict. | **PASS**: 54344 discovered / 48502 passed / **1** / 27 XFAIL / 5808 unsupported / 6 skipped, `EXIT=1`. The single failure is `Clang :: Format/dump-config-objc-stdin.m` — [`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config), expected on this branch and nothing else — so this is the baseline **+1 discovered / +1 passed**. Gated with the file in the tree, then committed. | same |
| evidence-debt | 2026-09-06 | `unicode-operators-experiment`, `e88b87bef8b7` | `SemaCodeComplete.cpp` `getDeclPriority` +1 (`DeclarationName::CXXUserOperatorName` joins the `CCP_Unlikely` chain) and `clang/test/CodeCompletion/unicode-operator-priority.cpp` (+51). | **PASS, `EXIT=0`, 0 failures**: 54184 discovered / 48296 passed / **0** / 27 XFAIL / 5855 unsupported / 6 skipped. Baseline **+1 discovered / +1 passed**. Both directions checked: on the pre-fix binary the two member user operators print `(35)` and the test fails on exactly those two `CHECK` lines; after, `(80)`, beside `operator+`. `git-clang-format --diff` with the in-tree binary: *clang-format did not modify any files*. | same |
| evidence-debt | 2026-09-06 | `unicode-operators-upstream`, `c0e07f78e679` | The same two files, **byte-identical patch** (verified by diffing the two `git show` outputs). | **PASS, `EXIT=0`, 0 failures**: 54242 discovered / 48324 passed / **0** / 27 XFAIL / 5885 unsupported / 6 skipped. Baseline **+1 discovered / +1 passed**. U20's headline invariant holds, **measured the right way**: `git log -p upstream/main..unicode-operators-upstream \| grep -ic backtick` is **0** across all 19 branch commits. The two-dot `git diff` form now returns 6, all of them upstream's own drift (`lldb/source/Interpreter/Options.cpp`, `llvm/docs/SphinxQuickstartTemplate.md`) because `upstream/main` has moved ahead of the branch base; `REPLAY.md` records the restatement. | same |
| evidence-debt | 2026-09-06 | *lldb, both Unicode branches* — no branch commit | Build configuration only. `lldbPluginExpressionParserClang` built and linked for `unicode-operators-experiment` in `~/src/llvm/build-cir-scratch` (BL04's tree, `lldb` added to `LLVM_ENABLE_PROJECTS`, host `clang++-23`) and for `unicode-operators-upstream` in a fresh `~/src/llvm/build-lldb-scratch-upstream` (host `/usr/bin/c++`, i.e. GCC), **since the two branches sit on different upstream bases and 367 files differ under `lldb/`** — `ClangASTSource.cpp` being byte-identical is not enough. | **PASS both, `EXIT=0`, and the load-bearing result is `-Wswitch`: zero occurrences in either build.** The switch the hunk joins is exhaustive over `DeclarationName::NameKind`, so a missing case would warn. The CIR tree's build is diagnostic-free entirely; the GCC-hosted one has 87 warnings from upstream code, one of which is on `ClangASTSource.cpp:238` (`-Wcast-qual` on `(void *)decl` in `FindCompleteType`) and is present identically in `~/src/llvm/main` — 113 lines from the hunk and nothing to do with it. **So the hunk compiles clean under both GCC and Clang.** The upstream scratch tree was then **deleted**, because its 2247 directories were what exhausted the machine's inotify budget; the configure command is in the handoff. | same |
| evidence-debt | 2026-09-06 | `docs/pattern-syntax-audit.py`, `docs/ucd-17.0.0.sha256` (new), `docs/unicode-operators.md` §4, `docs/backtick-operator-design.md` §17.4, `ops/BACKLOG.md`, `ops/DEVIATIONS.md`, `ops/unicode-operators/clang/REPLAY.md` | no branch — this repo only. The manifest in `sha256sum` check format; `--verify-manifest` (~55 lines) which `--emit-header` now implies; a reproducibility paragraph in U§4; a dated status-correction block in §17.4; four `Closed by` cells and one `Correction`; the new [`clang-slot-adl`](../BACKLOG.md#clang-slot-adl) row in the backlog and [`clang-slot-adl`](../DEVIATIONS.md#clang-slot-adl) in the ledger; a `REPLAY.md` section. | **PASS.** The five UCD inputs were re-fetched from unicode.org on 2026-09-06, all five hashes recorded from that fetch, `sha256sum -c` clean, and the regenerated `UnicodeOperatorCharSets.h` **byte-identical** to the committed one on both Unicode branches (`52ccbd15e26b…`). Refusal tested four ways: a one-comment-line edit to `PropList.txt` aborts with **0 bytes on stdout**; a missing input aborts; `--no-verify-manifest` still derives; a plain audit run is unchanged. Links: 650 in the six edited files, **0 broken**. | same |
| reconcile-implementation-cost | 2026-09-06 | `docs/unicode-operators.md` **U§8** — the opening paragraph, the Clang bullet list (3 bullets → 8), the flag paragraph, and two new slug-headed subsections | no branch — this repo only. The Clang list becomes *Lexer*, *Parser, using*, *Parser, declaring* (new), *The name tables — `DeclarationName`* (rewritten with the packed-against-Objective-C finding and the identity dividend), *The expression node* (new), *Serialization, modules and tooling* (new), *The code generator* (new, with the CIR instruction-for-instruction identity), and the existing *static analyzer* bullet, which gains a clause saying its seven sites are **within** the 43 and not additional. New: [dispatch-obligation-taxonomy](../../docs/unicode-operators.md#dispatch-obligation-taxonomy) and [closed-table-sibling-pattern](../../docs/unicode-operators.md#closed-table-sibling-pattern). | **PASS on the docs gate** — all eight rows carry `**RECONCILED**` and name the section *and the paragraph*; no row is marked closed that is not, and the two whose recommendations reach another step's section are marked partly closed and say which half. **No build, no feature branch touched** (correct for this step); the four LLVM worktrees and the GCC one were *read* to verify counts and flag guards, and none was modified. | [reconcile-implementation-cost](handoffs/reconcile-implementation-cost.handoff.md) |
| reconcile-implementation-cost | 2026-09-06 | `ops/unicode-operators/clang/DEVIATIONS.md` (8 rows + the header), `ops/BACKLOG.md` ([`matcher-operator-name`](../BACKLOG.md#matcher-operator-name)) | Eight rows `OPEN` → `**RECONCILED**`, each naming its destination paragraph; the ledger's header count goes from *"two entries"* to *"ten"* and gains a warning that three rows carried counts that did not survive re-measurement. [`matcher-operator-name`](../BACKLOG.md#matcher-operator-name)'s `Closed by` is filled in as *designed, and cited as evidence*. | **PASS** — the Unicode ledger now stands at 10 `RECONCILED` / 14 `OPEN`, and the fourteen are exactly the rows belonging to [reconcile-declaring-using](steps/reconcile-declaring-using.md) and [reconcile-remainder](steps/reconcile-remainder.md) plus [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators). | same |
| reconcile-implementation-cost | 2026-09-06 | *measurement, no file* — `unicode-operators-upstream` @ `c0e07f78e679`, plus flag-guard checks on all four Clang worktrees | Re-derived both site counts from the tree rather than from the ledger. `DeclarationName::NameKind`: **34 sites / 17 files** (31 `case` arms in 15 files, 1 TableGen `PropertyTypeCase`, 2 `\|\|` chains). `Stmt::StmtClass` and friends: **43 sites / 35 files**, categorised 6 link / 8 unreachable / 2 `-Wswitch` / 19 silent / 4 configuration-latent / 3 unforced-tooling / 1 off-axis, which sums. `UnqualifiedIdKind`: **12 sites / 5 files**, 7 `case` arms and 5 `==` tests. | **PASS** — the recorded 6/8/1/13 = 28 reproduces exactly against the tree as of `U16`; the later totals do not, and the ledger now says so. `ShouldParseIf<cplusplus.KeyPath>` confirmed present on `defm backtick` and/or `defm unicode_operators` in all four worktrees. | same |
| clang-slot-adl | 2026-09-06 | `backtick-trunk`, `9504b2c1fc51` | `Parser::TryParseBacktickCalleeSlot` (`clang/lib/Parse/ParseExpr.cpp`, +52) and its declaration in `Parser.h`, called from the slot before the `ParseExpression` fall-through; a bare unqualified name — plain identifier **or** template-id over one — is built with `Sema::ActOnIdExpression(..., HasTrailingLParen=true)`, so it reaches `BuildCallExpr` as an `UnresolvedLookupExpr`. Anything else falls through with the token stream untouched (a `TentativeParsingAction` reverts the lookahead). New `clang/test/SemaCXX/backtick-adl.cpp` (+165), eight sections. `backtick-semantics.cpp`'s prolog and section-2 heading corrected — they claimed ADL coverage the file never had. | **PASS, `EXIT=0`, 0 failures**: 54111 discovered / 48225 passed / **0** / 27 XFAIL / 5853 unsupported / 6 skipped. Exactly +1 discovered / +1 passed over the Baselines, from the one new lit file. **Two earlier runs of the same gate failed 2 `DirectoryWatcherTest` cases each — a *different* two each time, every one `No space left on device : inotify_add_watch()`.** `cloud-drive-dae` was holding **523,713 of 524,288** watches, leaving 196; the 8 cases pass standalone on this binary and on the pristine `build-main` one. Not filtered, not budgeted, re-run until the environment allowed a reading. | [clang-slot-adl](handoffs/clang-slot-adl.handoff.md) |
| clang-slot-adl | 2026-09-06 | `backtick-23`, `3a03487496a7` | Cherry-pick of `9504b2c1fc51`, **no conflict** — the slot's neighbourhood and `ParseUnqualifiedId`'s signature are identical on the two bases — and the two patches verified identical apart from blob hashes. | **PASS**: 54345 discovered / 48503 passed / **1** / 27 XFAIL / 5808 unsupported / 6 skipped, `EXIT=1`. The single failure is `Clang :: Format/dump-config-objc-stdin.m`, [`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config), which fails identically on the pristine binary and on this branch only. Exactly +1 / +1 over the Baselines. | same |
| clang-slot-adl | 2026-09-06 | `docs/backtick-operator-design.md` §17.4; `ops/DEVIATIONS.md`; `ops/BACKLOG.md`; `ops/completion/PLAN.md`; `ops/completion/steps/backtick-paper.md` | no branch — this repo only. §17.4's **status-correction block is removed** and its implementation-status paragraph rewritten, now naming both compilers' mechanisms; three paragraphs added under it — both compilers needed two attempts and the failures were the same failure; **the within-compiler control** (Unicode right, backtick wrong, one Clang build, one difference); and the near-miss, with *augmentation* named as the only shape whose failure is silent. [`clang-slot-adl`](../DEVIATIONS.md#clang-slot-adl) → **FIXED and RECONCILED**, naming §17.4 and each of the four paragraphs; [`clang-slot-adl`](../BACKLOG.md#clang-slot-adl)'s `Closed by` filled. The step was **inserted into Phase D and the ordinals below it renumbered**; [backtick-paper](steps/backtick-paper.md) gains it as a met dependency and a bullet saying what to write. | **PASS** — every claimed row marked with its destination paragraph; no "step N" cross-reference anywhere. | same |
| reconcile-declaring-using | 2026-09-06 | `docs/unicode-operators.md` U§7, U§7.1, U§4, §2 | `feccd46` — no branch, this repo only. U§7 "Declaring" rewritten around a five-row table of [over.oper]'s restrictions and the rule behind them; the [over.oper]p8 waiver and its cross-fixity consequence stated outright; U§7 "Using" split into one-candidate-set / ADL-inherited-by-not-writing-code / no-built-in-candidates-as-a-non-mechanism; U§7 "Desugaring" softens *"without modification"* and states the two-case sequencing rule; U§7.1 drops **"anywhere"** and carries the dependent-template exception with the accurate reason; U§4 gains the two-Unicode-versions paragraph. §2 gains a new decision entry, [static-member-operators](../../docs/unicode-operators.md#static-member-operators), and dated `Log.` entries on four existing ones — including one that **strikes a false clause in place** rather than deleting it. | **PASS on the docs gate.** Every claim re-derived on the pre-built `unicode-operators-upstream` binary rather than quoted: the static-member guard, the defaulted-prefix use and its ambiguity, the variadic waiver, the postfix diagnostic, the dependent template-id rejection with its non-dependent controls, the one-set ranking both ways round, the absent built-in candidates, member-before-right sequencing and the `-Wunsequenced` triple, and 14/14 disjointness unit-test cases against tables labelled Unicode 18.0. **No site count is quoted in U§7** — where size is the point it links to U§8. | [reconcile-declaring-using](handoffs/reconcile-declaring-using.handoff.md) |
| reconcile-declaring-using | 2026-09-06 | `papers/unicode-mathematical-operators.md` | `e1bdd07` — **two sentences the author's answers had already discarded were sitting in the paper, and no step had gone to look.** The static-member bullet gave the reason inspection disproved (the arity rule counts operands, so a two-parameter static member would be accepted; the rejection is a guard, and the reason is the two spellings). The *"What is not resolved"* paragraph carried the struck clause *"a limitation user-defined literal operators have had since C++11"*, which [mangling-abi](handoffs/mangling-abi.handoff.md) had forbidden in four places — all four being places a *future* writer would look. | **PASS.** `grep -rn "since C++11" papers/` is empty; no internal identifier was introduced and one pre-existing `U§4` reference was removed with the bullet it was in. The rule for the paper steps: when an answer changes a *reason*, grep the papers for the old reason — it is prose, so no identifier sweep finds it. | same |
| reconcile-declaring-using | 2026-09-06 | `ops/unicode-operators/clang/DEVIATIONS.md` (8 rows + the header), `ops/BACKLOG.md` ([`dependent-template-operator-id`](../BACKLOG.md#dependent-template-operator-id)) | Seven rows `OPEN` → `**RECONCILED**`, each naming its destination paragraph, plus the outstanding U§7 half of [`operator-candidate-assembly`](../unicode-operators/clang/DEVIATIONS.md#operator-candidate-assembly), whose parenthetical now reads *both halves*. [`operand-sequencing`](../unicode-operators/clang/DEVIATIONS.md#operand-sequencing) is marked **recorded as an open CWG question**, in those words. [`infix-parse-cost`](../unicode-operators/clang/DEVIATIONS.md#infix-parse-cost) and [`prefix-arity-selection`](../unicode-operators/clang/DEVIATIONS.md#prefix-arity-selection) are marked partly closed: their U§6 material is [reconcile-remainder](steps/reconcile-remainder.md)'s and two steps editing one paragraph is how a sentence gets lost. | **PASS** — the Unicode ledger now stands at **17 `RECONCILED` / 7 `OPEN`**, and the seven are [reconcile-remainder](steps/reconcile-remainder.md)'s six plus [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators). 2035 local Markdown links checked repo-wide, 0 broken. **A fourth recorded mechanism failed re-checking:** `Sema::ActOnUserOperator`, not `CreateOverloadedUserOp`, performs the operator-name lookup; the header now warns about mechanisms as well as counts. | same |
| reconcile-remainder | 2026-09-06 | `docs/unicode-operators.md` U§5, U§6, U§8, U§10, §11's restored heading, U§12, U§13, U§13.1, §2 | no branch — this repo only. **U§5**: predicate 5 becomes the 3/13/12 enumeration with the middle-dot and fraction-slash corrections, plus the new decision entry [confusable-spelling-provenance](../../docs/unicode-operators.md#confusable-spelling-provenance) (curated, not derived, with the principle and the *is confusable with* / no-fix-it consequence). **U§6**: the contested *"parsing is the easy part … easier even than backtick"* sentence rewritten as a three-bullet split plus a paragraph on why a design document makes that error; the structural one-liner (two parser functions, no disambiguation state, against §5's suppression flag); and the missing sixth worked example `⊖a ⊞ 2 * ⊖b`. **U§8**: paragraph 2 rewritten around the emittable-iff-not-an-identifier-constituent rule, and the classification/identity split for UCN spellings; [closed-table-sibling-pattern](../../docs/unicode-operators.md#closed-table-sibling-pattern) gains its fifth row. **U§10**: the clang-format bullet measured. **U§12**: separable fates as an executed result with the base pinned, the three coupled constructs, the not-a-shared-AST paragraph, the formatting comparison, the postfix routing bullet, and the *maturity differs* bullet corrected from "none" to one implementation. **U§13**: the fold bullet, and the adjacency question marked answered. **§11's `## 11. Prior art` heading restored** — silently deleted by `a9fdb73`, which is why ledger rows cite a section that had ceased to exist. | **PASS** (docs gate) — every row names its destination section **and paragraph**; nothing was built and no feature branch was touched. | [reconcile-remainder](handoffs/reconcile-remainder.handoff.md) |
| reconcile-remainder | 2026-09-06 | `docs/backtick-operator-design.md` §6, §11, §17, §17.3, §17.7 (new), §17.8 (new), §3 [type-name-slot](../../docs/backtick-operator-design.md#type-name-slot) | no branch — this repo only. §6's site list gains **the analysis layer (7 sites)**, **the code generator (4 arms)** and a pointer to the priced type slot, and its test item gains the diff-at-more-than-one-configuration line. §11 phase 2's *"purely additive"* is corrected in place, with the generalization that **the transparency is what costs, not the node**. §17.3 is rewritten: the type slot is a second production with a disambiguation rule and a measured price, not a blessed consequence, and [type-name-slot](../../docs/backtick-operator-design.md#type-name-slot)'s old clause is **struck in place** in a dated `Log.` §17.7 states the back-end symmetry — same four arms, same files, same failure modes, identical generated code — as the front-end/back-end counterpart of U§12's not-a-shared-AST paragraph. §17.8 says which of §§17.5–17.7 has no GCC counterpart **by construction**, and separates that from the one real gap (the type slot). | **PASS** (docs gate) — same. | same |
| reconcile-remainder | 2026-09-06 | `papers/unicode-mathematical-operators.md`, `papers/backtick-infix-and-keyword-escape.md` | no branch — this repo only. **A third step in a row found a paper carrying a claim its own answers had discarded.** `unicode-mathematical-operators.md`'s *Fold expressions* section called the exclusion *inherited from sharing the level* and *an open question*: the author decided it on 2026-09-06 — deliberate, one answer for both features — and the section is rewritten to say so, with the exclusion's forward-compatibility and the silent-failure note for implementers. Its *What is not resolved* list loses the fold bullet. Its formatting section's *"clang-format required 32 lines"* becomes about forty across three files, with the canonicalization result. Its exclusions section gains the curated-not-derived claim and the principle. `backtick-infix-and-keyword-escape.md` gains the fold sentence its decision owed it, in *The productions*. **No internal identifier was introduced** in either. | **PASS** — the identifier sweep over `papers/*.md` and `docs/*.org` finds only the four pre-existing `U§` references [unicode-paper](steps/unicode-paper.md) already owns. | same |
| reconcile-remainder | 2026-09-06 | `ops/unicode-operators/clang/DEVIATIONS.md` (10 rows + a rewritten header), `ops/DEVIATIONS.md` (4 rows), `ops/BACKLOG.md` ([`confusable-spellings`](../BACKLOG.md#confusable-spellings)), `ops/unicode-operators/clang/REPLAY.md` §5, `ops/unicode-operators/clang-experiment-plan.md` | no branch — this repo only. Ten Unicode rows marked (the six listed, [`postfix-operators`](../unicode-operators/clang/DEVIATIONS.md#postfix-operators), and the three left partly closed) and four backtick rows; the Unicode ledger's header rewritten from a 17/7 count to *all twenty-four marked, none open*, with the mechanism warning raised from three rows to five and the count warning from three to four. `REPLAY.md` §5's two ordering traps are struck **in place** with their corrections attached, because the existing correction sat three hundred lines below the table a replay agent reads. The experiment plan's one-construct replay assumption is struck and replaced by the numbered three, with the executed result and the pinned-base caveat. | **PASS** — `grep 'Status:\*\* OPEN'` over all three ledgers returns nothing; a per-entry sweep shows 11 backtick / 10 GCC / 24 Unicode rows, every one carrying a marked status. | same |
| hygiene-parity | 2026-09-07 | `backtick-trunk` | `c0d69702b6d7` — `backtickInfixExpr()` across `ASTMatchers.h`, `ASTMatchersInternal.cpp`, `Dynamic/Registry.cpp` and **both** `TK_IgnoreUnlessSpelledInSource` traversal sites in `ASTMatchFinder.cpp`, with `LibASTMatchersReference.html` **regenerated** by `docs/tools/dump_ast_matchers.py`; one `case Stmt::BacktickInfixExprClass:` in `MakeCXCursor`; `err_backtick_nested_requires_parens` **deleted**; the slot-interior `SplitPenalty` bump in `TokenAnnotator::calculateFormattingInformation` (flat 100, infix pair only — the escape slot is one token). Three new gtest cases; two test files gained comment-only corrections; three `// D8:` comments renamed to the slug they implement. | **PASS** — **54114 / 48228 / 0**, XFAIL 27, skipped 6, unsupported 5853. Baseline 54111/48225/0, so **+3/+3 and nothing else**. The step's own `-Wswitch` bullet: a full rebuild of `clang` + `AllClangUnitTests` (493 targets) has `grep -c warning` = **0**, against exactly one before the fix. | [hygiene-parity](handoffs/hygiene-parity.handoff.md) |
| hygiene-parity | 2026-09-07 | `backtick-23` | `69ac49d3e209` — cherry-pick of `c0d69702b6d7`, clean, **and the generated HTML re-run through the generator anyway**: `dump_ast_matchers.py` on the 23.x tree reproduces the cherry-picked file byte-for-byte, `git status` clean. A generated file that cherry-picks cleanly is not evidence that it is what the generator produces, which is what the gate actually tests. | **PASS** — **54348 / 48506 / 1**, the 1 being [`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config), permanent and identical on the pristine `build-main` binary. Baseline 54345/48503/1, so **+3/+3 and nothing else**. `ninja check-clang` exits 1 on this branch by design; the exit code was read from the log. Zero build warnings. | same |
| hygiene-parity | 2026-09-07 | `docs/backtick-operator-design.md` §6 items 6 and 7, §7; `docs/unicode-operators.md` §8 | no branch — this repo only. **§6 item 6** rewritten: two diagnostics, not three, with the paragraph saying why the third was withdrawn rather than deleting the mention — *a diagnostic that cannot fire is a claim the grammar has already withdrawn*. **§6 item 7** gains its second paragraph, *the tooling surface*, where [reconcile-remainder](handoffs/reconcile-remainder.handoff.md)'s forward note placed it: libclang's silent `-Wswitch`-only report, ASTMatchers' complete absence of any report, and the generated doc's gating test. **§7's break-policy bullet** gains the measured reach of the bump and the named limit. **U§8**'s `TemplateIdAnnotation` sentence gains the two nulls, the measured diagnostics and the control. | **PASS** (docs gate) — every row names its destination section *and* paragraph; 2310 local Markdown links checked repo-wide, **0 broken**. | same |
| hygiene-parity | 2026-09-07 | `ops/BACKLOG.md` (5 rows), `ops/DEVIATIONS.md` ([`bare-nesting-detection`](../DEVIATIONS.md#bare-nesting-detection)) | no branch — this repo only. Five `Closed by` cells filled. **Two of them said `BL06`** — a step that never ran and was superseded by this plan — and now name the step that actually closed them. [`bare-nesting-detection`](../DEVIATIONS.md#bare-nesting-detection) gains a dated `Log.` and keeps **RESOLVED**: it was resolved on paper in the other direction, and is now resolved in the source as well. | **PASS** — **no new deviation row, and all three ledgers still read zero open** (11 / 10 / 24, every one marked). No internal identifier was introduced into `papers/`, which were checked against the deleted diagnostic and deliberately left unedited. | same |
| backtick-paper | 2026-09-07 | `papers/backtick-infix-and-keyword-escape.md` | no branch — this repo only. **Implementation experience rewritten and the build repaired.** The ADL subsection said the divergence was GCC's; it was both compilers', the same way, and now says so, with the within-compiler control (one Clang build carrying two infix features, one inheriting ADL and one not) and the augmentation shape named as the only ADL test whose failure is silent. New: *What is implemented, and what is not* (the escape's real position set, the type slot's single-compiler evidence, the two round-trip exceptions), *What the AST node costs, and which compiler pays it* (seven analyzer sites, 2-vs-8 path notes, and why GCC pays none of it), *The gate is the part that fails quietly* (the C-mode acceptance and GCC's fall-through arms). Branch links corrected to `backtick-23` / `backtick-trunk`; the `backtick-operator` grammar block given the two type productions the wording already had; the fold sentence stops quoting one compiler's diagnostic text. Front matter's `header-includes` moved into the body. | **PASS** — **57 implementation claims re-derived** on the built `backtick-trunk` `clang++`/`clang`/`clang-format`, the experiment-branch `clang++`, and `cc1plus`; **5 failed** (4 rows; two of them are one defect seen twice) and are recorded rather than argued away. `make backtick-infix-and-keyword-escape.html` and `make backtick-infix-and-keyword-escape.pdf` both build (18 pp.); the PDF **did not build before this step**. No internal identifier: two sweeps, one for retired serial forms and one against all 143 live slugs, both clean. | [backtick-paper](handoffs/backtick-paper.handoff.md) |
| backtick-paper | 2026-09-07 | `docs/infix-backtick-operator.org` + `.meta` | no branch — this repo only. Blog register. The false *"a type name is callable, so a type in the slot constructs"* replaced by the account of getting it wrong (it is a second production with a disambiguation rule, and the first implementation rejected every type-slot shape). Three new sections — *What I got wrong* (the ADL story and the within-compiler control), *What the AST wrapper costs*, *What isn't done yet* — plus corrected branch links and the re-sync datum. `.meta` date 2026-06-29 → 2026-09-07, the content having moved substantially. | **PASS** — `lexcheck --register blog` **clean** (was: one wager-frame error, two workhorse warnings, dash saturation at 114/10k → 20/10k); org exports through `org-html-export-to-html` with no errors. | same |
| backtick-paper | 2026-09-07 | `docs/backtick-operator-design.md` §3, §12, §17.5, §17.8; `ops/DEVIATIONS.md`, `ops/gcc/DEVIATIONS.md` | no branch — this repo only. Four rows opened, all `OPEN` and none owned: [`escape-name-positions`](../DEVIATIONS.md#escape-name-positions), [`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape), [`escape-alias-name-parity`](../gcc/DEVIATIONS.md#escape-alias-name-parity), [`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling). Design doc corrected where the paper's claims came from it: §17.8's *one place* the compilers disagree is **two**, with a third divergence in diagnostics kept separate; §17.5's *three shapes* the wrapper recognises is **four**; §12 gains the measured position table; [keyword-escape-printing](../../docs/backtick-operator-design.md#keyword-escape-printing) gains the single-compiler note. | **PASS** — 2339 local Markdown links repo-wide, 0 broken; the three ledgers read 2 / 2 / 0 open rows, up from zero, each with its measurement and its recommended doc change. | same |
| unicode-paper | 2026-09-07 | `papers/unicode-mathematical-operators.md` | `cdbc86e` — no branch, this repo only. **Every measurable claim re-derived against the built compilers; 89 checked, 14 failed, all 14 corrected.** The exclusions section carries the 3/13/12 split and the **thirteenth confusable, U+2044, which the paper had left out of a list the shipped table contains**. Volume re-measured against the branch base (86 / +2033 / −18 production, 34 / +5416 tests, 120 files, **19** commits, not 15); the ledger is **24** rows and the track **22** steps. The 67-site dispatch table becomes **89 across three axes that share no site**, with the node's 43 given [dispatch-obligation-taxonomy](../../docs/unicode-operators.md#dispatch-obligation-taxonomy)'s seven categories, dated and branch-stamped, summing to the total. *Three-for-three* becomes **five**, three siblings and two refusals, ending on the leak-into-`operator+` argument stated structurally. New sections: the desugaring checked in **both** code generators (five shapes, instruction-identical to the hand-written call), and the design document's own cost estimate reported as wrong *in the direction design documents always get it wrong*. U§9 rewritten to the author's ruling: fallback first, then the production **as a request**, letters the ABI group's, `s` reserved. *What is not resolved* keeps only what someone else must answer. | **PASS — all four gate bullets.** The paper renders under its real number, **D4345R0**, and `grep -rIn -i dxxxx` over the repo returns **only** the historical narrative inside `ops/completion/steps/unicode-paper.md`, which now carries a dated note saying so; no live document, no path and no link. `make -C papers unicode-mathematical-operators.html unicode-mathematical-operators.pdf` both build, **14 pp., zero missing characters** (136 before this step) and **zero warnings** — the *unrecognized document number* warning the placeholder produced is gone. `lexcheck --register formal` clean; every appositive tail in the file predates this step. **2420 local Markdown links repo-wide, 0 broken**, and every `papers/*.md` path resolved against the filesystem — the only non-existent ones are the two old names and two worked examples, all of them prose about naming rather than pointers | [unicode-paper](handoffs/unicode-paper.handoff.md) |
| unicode-paper | 2026-09-07 | `docs/unicode-infix-operators.org` + `.meta`, `docs/unicode-operators.md` | `cdbc86e` — no branch, this repo only. Blog register. The post **said nothing is implemented**, which had been false since the prototype was built; it now opens on the build and carries five new movements — the parse result stated structurally (two productions, two functions, no disambiguation state at all), the under-priced-cost correction with the reason design documents make it, the 28-code-point exclusion list with the curated-not-derived principle and why the message says *is confusable with*, *Two things the build told me* (the UCN identity trap that no token-level test can see, and ADL arriving as a present), and *What the node costs* with the 89-site fan-out and the spelled-not-dispatched finding. *What isn't done yet* is new: one compiler, Windows, sequencing, postfix, v2. `.meta` 2026-08-02 → **2026-09-08**, a day after the backtick post, since they publish as neighbours. One typo fixed in `docs/unicode-operators.md` §7: `⊠5` for `⊟5` in the ambiguity example. | **PASS** — `lexcheck --register blog` **clean**; em-dashes 7.3/10k, below both the paper rate and the backtick post's; org exports with no errors and the generated HTML was deleted (`docs/` is source only). Public text: two sweeps over both deliverables — retired serial forms and internal paths, then all **145** live repo slugs — **zero real hits**, the only match being the Nikola `.. slug:` key, which is a URL. | same |
| unicode-paper | 2026-09-07 | `papers/` — **both papers renamed**, `CLAUDE.md` Layout, `ops/completion/steps/unicode-paper.md` | no branch, this repo only. The author supplied **D4345R0 — *Extending C++ with Unicode Mathematical Operators*** and authorised the second rename, so the two papers leave this sitting consistent with each other and with `~/.claude/CLAUDE.md`'s rule that a paper file is named for what it *is*: `papers/dxxxxr0.md` → **`papers/unicode-mathematical-operators.md`**, `papers/d4307r0.md` → **`papers/backtick-infix-and-keyword-escape.md`**. `d4345r0.md` was rejected as a target — replacing a placeholder number with a real one satisfies nothing and entrenches the convention's own counterexample. The backtick paper's basename is deliberately **not** `infix-backtick-operator`, which is its blog post's name; it carries the token and both jobs so the paper and the post are not confusable. Number and title live in the front matter and the prose, unchanged apart from the two front-matter fields. Every path reference rewritten repo-wide — 15 files for the Unicode paper, 14 for the backtick one — plus `CLAUDE.md`'s Layout, which now lists `papers/` explicitly with both numbers, both build traps, and the naming rule. | **PASS** — **2420 local Markdown links repo-wide, 0 broken.** Every `papers/*.md` path was resolved against the filesystem; the only non-existent ones are the two old names and two worked examples, all in prose about naming rather than in pointers. **Both papers build in both formats after the rename** and neither regressed: Unicode 14 pp., 0 missing characters, 0 warnings; backtick 18 pp., PDF still building after its `header-includes` fix. The four surviving `DXXXX` tokens are inside this step's own step file, which now opens with a dated note saying they describe the state the step was given. | [unicode-paper](handoffs/unicode-paper.handoff.md) |
| unicode-branch-maintenance | 2026-09-07 | `unicode-operators-experiment` | `7278a2985659` — **merge commit**, `git merge --no-ff c0d69702b6d7`, bringing the four commits `backtick-trunk` gained since M2: [null-return-suppression](handoffs/null-return-suppression.handoff.md)'s backtick half (`c1c6af4dd620`), [evidence-debt](handoffs/evidence-debt.handoff.md)'s template `-ast-print` test (`2b7471f6bc13`), [clang-slot-adl](handoffs/clang-slot-adl.handoff.md) (`9504b2c1fc51`) and [hygiene-parity](handoffs/hygiene-parity.handoff.md) (`c0d69702b6d7`). 17 files, **+484/−14**, and **all 14 deleted lines are backtick text** — no Unicode line moved. **Seven conflicts, none semantic:** four are `U17`'s node and hygiene-parity's node wanting one anchor (`ASTMatchers.h`, `ASTMatchersInternal.cpp`, `CXCursor.cpp`, and `ASTMatchFinder.cpp`'s two split sites) → *keep both, backtick after Unicode*; two are test blocks sharing a closing brace (`ASTMatchersNodeTest.cpp`, `FormatTest.cpp`) → keep both, with the Unicode block's brace restored; one is already-applied and this branch says more (`BugReporterVisitors.cpp` peels both wrappers) → ours. `Registry.cpp` did not conflict, as predicted. **The branch's backtick slot had no ADL until this merge** and now has `TryParseBacktickCalleeSlot`. | **`check-clang` GREEN, unfiltered, `EXIT=0`** — **54189 / 48301 / 0**, XFAIL 27, unsupported 5855, skipped 6, 413.11 s. Baseline 54184/48296/0, so **+5/+5 and nothing else** (2 lit files + 3 gtest cases). Build `EXIT=0`, 948 targets, **zero warnings**. The fold guard was re-verified by **deleting** `Level != prec::UserInfix`, rebuilding, and watching `Parser/unicode-operator-precedence.cpp` fail on **line 322 only** — then restoring it. 17 targeted lit tests and the four ASTMatchers gtest cases pass. The **first** run failed three `DirectoryWatcherTest.*` cases, [`inotify-watch-budget`](../BACKLOG.md#inotify-watch-budget) at 523,730 of 524,288 held by `cloud-drive-dae`; confirmed on the pristine `build-main` binary, **not filtered, not budgeted**, re-run. | [unicode-branch-maintenance](handoffs/unicode-branch-maintenance.handoff.md) |
| unicode-branch-maintenance | 2026-09-07 | `unicode-operators-upstream` | `8c2a90f56b00` — comment only, +6/−4 in `clang/lib/Sema/SemaDeclCXX.cpp`. `CheckUserOperatorDeclaration`'s `isStatic()` guard carried the reason [over-oper-restrictions](../../docs/open-decisions.md#over-oper-restrictions) rejected: the arity rule counts **operands**, so a two-parameter static member satisfies it and the rejection is the guard's, not the rule's. **M2's wording was copied verbatim from `de76585ae45d`, not written again** — verified by extracting the block from both worktrees and diffing them — so the one-paragraph divergence M2 left between the two Unicode branches is closed. The guard is untouched and the merge above **did not** and must never reach this branch. | **`check-clang` GREEN, unfiltered, `EXIT=0`** — **54242 / 48324 / 0**, XFAIL 27, unsupported 5885, skipped 6, 480.30 s: **exactly the Baselines row**, which is the expected result for a comment-only commit. Build `EXIT=0`, 16 targets, zero warnings. Two earlier runs failed 3 and then 1 `DirectoryWatcherTest.*` case — **a different subset each time**, the signature of the environment rather than a regression; the green run followed a standalone `[  PASSED  ] 8 tests` on `build-main`. | same |
| unicode-branch-maintenance | 2026-09-07 | `ops/unicode-operators/clang/REPLAY.md`, `ops/completion/PLAN.md`, `ops/unicode-operators/clang/PLAN.md` | no branch — this repo only. A new `REPLAY.md` section classifying the merge **`backtick dependency` — unreplayable, and never to reach `unicode-operators-upstream`** — with the re-run contamination scan (same six production files, same four comment-only entries, the one addition predating this merge), the re-measured 121 files / 221 hunks / +7701−57, the fold-guard deletion result, and **a new hazard**: four tooling anchors now carry two arms on the experiment branch and one upstream, so a replay must copy the `UserOperatorExpr` *arm* and never the region. | **PASS** — no ledger row changes classification, and that is measured rather than argued: every one of the merge's 14 deleted lines is backtick text. | same |
| settle-paper-rows | 2026-09-07 | `backtick-trunk` | `3b2103895224` — the fourth inner shape a type slot can build. One arm in `BacktickInfixExpr::getOperand` (`clang/lib/AST/Expr.cpp`) and one in `StmtPrinter::VisitBacktickInfixExpr`, both matching `CXXFunctionalCastExpr` over `CXXParenListInitExpr`, taking the **user-specified** initializers because the full list carries defaulted members beyond the two operands, and printing the type from the semantic node as the constructor arm does. Cases added to the two existing files that own these claims — `clang/test/Parser/backtick-infix.cpp` (the range, literal columns) and `clang/test/Parser/backtick-ast-print.cpp` (the printing) — so **no test count moves**; both files pinned to `-std=c++20`, including the second ast-print RUN line that re-parses what the first printed, since parenthesized aggregate initialization is C++20. Closes [`type-slot-aggregate-shape`](../DEVIATIONS.md#type-slot-aggregate-shape). | **`check-clang` GREEN, unfiltered, `EXIT=0`** — **54114 / 48228 / 0**, XFAIL 27, unsupported 5853, skipped 6: **exactly the Baselines row**, and the expected result for cases added to existing files. **Verified failing first on the pre-fix `build-backtick` binary**, which was still unpatched: the range came back `<col:19, col:22>` against the written `<col:16, col:24>`, and the printing came back `Agg(1, 2)` and — the part the row understated — `(aggT<int>)(3, 4)`, a cast applied to a comma expression, which is a **different program** and would not have re-parsed. | [settle-paper-rows](handoffs/settle-paper-rows.handoff.md) |
| settle-paper-rows | 2026-09-07 | `backtick-23` | `d93d6c520e6f` — cherry-pick of `3b2103895224`, clean, gated independently. | **`check-clang` GREEN against the Baselines, `EXIT=1` for the one permanent failure** — **54348 / 48506 / 1**, the 1 being [`stray-clang-format-config`](../BACKLOG.md#stray-clang-format-config) (`Clang :: Format/dump-config-objc-stdin.m`), which fails identically on the pristine `build-main` binary and is this branch's standing reading. XFAIL 27, unsupported 5808, skipped 6, 417.09 s. No count moved. | same |
| settle-paper-rows | 2026-09-07 | `backtick` (GCC) | `eb59d9b897e` — an escaped name is spelled escaped in diagnostics. `dump_decl_name` (`gcc/cp/error.cc`) prints `` `kw` `` when `flag_backtick` and the identifier is a keyword, which under the flag can only be an escaped name because `grokdeclarator` rejects the bare declarator-id. **The first build of this was wrong in the way the branch's previous commit warns about** and the guard is the finding: `cp_parser_error_1` hands a raw keyword token to `%qE` as though it were a name — with a comment in the source saying so — so `void new (int, int);`, containing no backtick, reported against a backticked spelling. `cp_printing_raw_token` (`gcc/cp/cp-tree.h`) marks that one call. Test cases appended to `g++.dg/backtick/escape-diag.C`, which already owns the flag-off parity half. Closes [`escape-diagnostic-spelling`](../gcc/DEVIATIONS.md#escape-diagnostic-spelling). | **PASS**: `dg.exp=g++.dg/backtick/*.C` → **109 passes / 0 failures**, against 103/0 before — **+6 = 2 new directives × 3 standards** (`gnu++98`, `gnu++20`, `gnu++29`). Flag-off parity measured rather than assumed: a backtick-free program's diagnostics are **byte-identical** with the flag on and off, and the intermediate build that was not is what the guard exists for. Four entity-naming surfaces verified by hand — an argument note, a member reference, *cannot be used as a function*, *no matching function for call to*. | same |
| settle-paper-rows | 2026-09-07 | `docs/backtick-operator-design.md`, `docs/open-decisions.md`, `papers/backtick-infix-and-keyword-escape.md`, `docs/infix-backtick-operator.org`, both backtick ledgers | no branch — this repo only. §17.5's *three shapes* is four and says why the general statement survives the fix; §17.3's *two printer arms* is three; §17.8 is *two kinds, four programs*, and its diagnostic-divergence paragraph now records a divergence that lasted one day, with what the fix cost on both compilers. §12's eight-position paragraph becomes a **nineteen-position table, measured in both compilers**, read as a map of two parsers rather than of one design. [keyword-escape-printing](../../docs/backtick-operator-design.md#keyword-escape-printing) and [keyword-escape-coexistence](../../docs/backtick-operator-design.md#keyword-escape-coexistence) each gain a dated `Log.`. **A sixth question page, [escape-name-positions](../../docs/open-decisions.md#escape-name-positions)** — the first backtick-side one and the only open one — with four options priced in both compilers. Paper and post corrected in three places each. | **BLOCKED on the author** for [`escape-name-positions`](../DEVIATIONS.md#escape-name-positions) and [`escape-alias-name-parity`](../gcc/DEVIATIONS.md#escape-alias-name-parity), which are one question; **PASS** on everything else. Both paper formats build, **0 missing characters**, `EXIT=0`. `lexcheck --register formal` on the paper: no new warning, all three at or below their pre-step counts; `--register blog` on the post: **clean**. Public text swept for internal identifiers — none. | same |
