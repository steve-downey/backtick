# Replay ledger — what can be lifted onto clean `main`

The experiment branch is based on `backtick-trunk`, which is not upstream.
The upstream Unicode patch must not require unlanded backtick changes
(U7, U12 — the two features must be able to have separate fates). This
ledger is how that stays true without anyone having to reconstruct it at
the end.

**Every step appends at least one row**, even if the row says "nothing
backtick-related". U19 audits the ledger against the actual diff; U20
executes it.

Classifications:

- `upstream replay` — lift the hunk onto clean `main` as-is.
- `backtick dependency` — works only because the backtick diff is present;
  a standalone equivalent must be written for `main`. Name it in the notes.
- `shared if landed` — reusable if backtick lands first; otherwise needs a
  standalone equivalent.

| Step | Files / hunks | Class | Standalone equivalent needed on `main` |
|------|---------------|-------|----------------------------------------|
| U00 | No source files touched. Establishes the base itself: worktree `/home/sdowney/src/llvm/unicode`, branch `unicode-operators-experiment` @ `bd6f4d5fa102`, which is `upstream/main` @ `bb33de72920a` **plus the 45-commit backtick diff**. | `backtick dependency` (the *base*, not any hunk) | U20 branches from `bb33de72920a` — or from whatever `upstream/main` is then — and replays only U01–U18 rows classed `upstream replay`. The 45 backtick commits are never replayed. Nothing to write; this row exists so U19 has the base pair recorded rather than reconstructed. |
| U01 | `clang/include/clang/Basic/LangOptions.def` (+1: `LANGOPT(UnicodeOperators, …)`); `clang/include/clang/Options/Options.td` (+4: `defm unicode_operators`); `clang/lib/Driver/ToolChains/Clang.cpp` (+4: `addLastArg(OPT_funicode_operators, OPT_fno_unicode_operators)`); new `clang/test/Driver/funicode-operators.c`. | `upstream replay` | **None.** Every hunk is independent of backtick by construction — the flag, the LangOpt, and the forwarding call are all new lines that neither read nor modify any backtick symbol. U20 can `git cherry-pick 3014f97cfc31` onto clean `main` unmodified. Two adjacency caveats, both cosmetic: each of the three hunks' diff *context* is the corresponding `-fbacktick` line, so the patch will not apply with zero fuzz to a `main` that lacks backtick — re-anchor each hunk (LangOptions.def: after `Reflection`; Options.td: after `defm reflection`, before `defm sized_deallocation`; Clang.cpp: after the `-fsized-deallocation` `addLastArg`). And the new driver test's `BOTH` RUN line uses `-fbacktick`; drop that one RUN line and the two `BOTH-DAG` lines on `main`, keeping `ON`/`OFF`/`DEFAULT`. |
| U02 | New `clang/lib/Lex/UnicodeOperatorCharSets.h` (generated, 182 lines); new `clang/unittests/Lex/UnicodeOperatorCharSetsTest.cpp`; `clang/unittests/Lex/CMakeLists.txt` (+1 source line). Generator-side: `docs/pattern-syntax-audit.py` in the plan repo (`--emit-header`). | `upstream replay` | **None — nothing backtick-related.** Pure data plus two flag-free lookup functions; no `-fbacktick`, no `LangOpts`, no backtick symbol is referenced, and the header is not included by any existing TU yet. `git cherry-pick 2389fe7be452` applies to clean `main` as-is: the only modified (non-new) file is `clang/unittests/Lex/CMakeLists.txt`, whose diff context is upstream text. The one thing U20 must *not* do is re-run the generator against a newer UCD to "refresh" the table — U1 is frozen at 17.0.0 by design; replay the generated bytes. |
| U03 | `clang/include/clang/Basic/TokenKinds.def` (+9: `TOK(user_operator)` after `TOK(raw_identifier)`); `clang/include/clang/Lex/Lexer.h` (+20: two `static uint32_t getUserOperatorCodePoint` declarations); `clang/lib/Lex/Lexer.cpp` (+1 include, +30 the two helper definitions, +8 the `tryConsumeIdentifierUTF8Char` early return, +14 the `LexTokenInternal` `default:` classification); `clang/unittests/Lex/LexerTest.cpp` (+74: six `UnicodeOperator*` cases); new `clang/test/Lexer/unicode-operators.cpp`. | `upstream replay` | **None — nothing backtick-related.** No hunk reads or modifies a backtick symbol; the only shared *file* with the backtick diff is `TokenKinds.def` (backtick added `PUNCTUATOR(backtick, "`")` at line 265, ~95 lines below the new `TOK`) and `Lexer.cpp` (backtick's only hunk there is `case '`':` at 4566, ~85 lines above the new `default:` hunk). Both are far enough apart that `git cherry-pick dacbe22ddeef` onto clean `main` should apply; if it fuzzes, re-anchor the `TokenKinds.def` hunk on `TOK(raw_identifier)` and the two `Lexer.cpp` hunks on `llvm::convertUTF8Sequence` / `if (isASCII(CodePoint) \|\| isUnicodeWhitespace(CodePoint)) return false;` — none of that context is backtick's. **One ordering constraint for U20:** the `Lexer.cpp` hunks call `isUserOperatorChar()`, so U02's generated header must be replayed first. |
